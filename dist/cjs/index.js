'use strict';

class TypedEventBus {
    constructor() {
        this.handlers = {
            tokensUpdated: new Set(),
            tokenChange: new Set(),
            reauthRequired: new Set(),
            authStateChanged: new Set()
        };
    }
    emit(event, payload) {
        for (const handler of this.handlers[event]) {
            handler(payload);
        }
    }
    on(event, handler) {
        this.handlers[event].add(handler);
        return () => {
            this.handlers[event].delete(handler);
        };
    }
    clear() {
        for (const key of Object.keys(this.handlers)) {
            this.handlers[key].clear();
        }
    }
}

class MemoryStorageAdapter {
    constructor() {
        this.data = new Map();
    }
    getItem(key) {
        return this.data.get(key) ?? null;
    }
    setItem(key, value) {
        this.data.set(key, value);
    }
    removeItem(key) {
        this.data.delete(key);
    }
}
const memoryFallback = new MemoryStorageAdapter();
const createBrowserStorageAdapter = (storage) => ({
    getItem: (key) => storage.getItem(key),
    setItem: (key, value) => storage.setItem(key, value),
    removeItem: (key) => storage.removeItem(key)
});
const getDefaultStorageAdapter = () => {
    if (typeof window !== 'undefined' && window.localStorage) {
        return createBrowserStorageAdapter(window.localStorage);
    }
    return memoryFallback;
};

const TOKEN_STORAGE_KEY = 'sanctum_sdk_tokens_data';
const createStorageTokenDataAdapter = (storageAdapter) => {
    return {
        async getTokenData() {
            const raw = await storageAdapter.getItem(TOKEN_STORAGE_KEY);
            if (!raw)
                return null;
            try {
                return JSON.parse(raw);
            }
            catch {
                return null;
            }
        },
        async setTokenData(tokensData) {
            await storageAdapter.setItem(TOKEN_STORAGE_KEY, JSON.stringify(tokensData));
        },
        async clearTokenData() {
            await storageAdapter.removeItem(TOKEN_STORAGE_KEY);
        }
    };
};

const CLIENT_KEY_STORAGE_KEY = 'sanctum_sdk_client_key';
const KEY_LENGTH = 8;
const generateClientKey = () => {
    const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
    let out = '';
    for (let i = 0; i < KEY_LENGTH; i += 1) {
        out += chars.charAt(Math.floor(Math.random() * chars.length));
    }
    return out;
};
class ClientKeyStore {
    constructor(storageAdapter, getClientKeyFromConfig) {
        this.storageAdapter = storageAdapter;
        this.getClientKeyFromConfig = getClientKeyFromConfig;
        this.cachedHookKey = null;
    }
    async getClientKey() {
        if (this.getClientKeyFromConfig) {
            if (this.cachedHookKey !== null) {
                return this.cachedHookKey;
            }
            const key = await Promise.resolve(this.getClientKeyFromConfig());
            if (typeof key !== 'string' || key.length === 0) {
                throw new Error('getClientKey() must return a non-empty string');
            }
            this.cachedHookKey = key;
            return key;
        }
        let key = await this.storageAdapter.getItem(CLIENT_KEY_STORAGE_KEY);
        if (!key) {
            key = generateClientKey();
            await this.storageAdapter.setItem(CLIENT_KEY_STORAGE_KEY, key);
        }
        return key;
    }
}

class SessionManager {
    constructor(tokenDataAdapter, events) {
        this.tokenDataAdapter = tokenDataAdapter;
        this.events = events;
    }
    async getTokenData() {
        return Promise.resolve(this.tokenDataAdapter.getTokenData());
    }
    async setTokenData(tokensData) {
        await Promise.resolve(this.tokenDataAdapter.setTokenData(tokensData));
        this.events.emit('tokenChange', tokensData);
        this.events.emit('tokensUpdated', tokensData);
        this.events.emit('authStateChanged', 'authenticated');
    }
    async clear() {
        await Promise.resolve(this.tokenDataAdapter.clearTokenData());
        this.events.emit('tokenChange', null);
        this.events.emit('authStateChanged', 'idle');
    }
    async updateTokensAfterRefresh(tokensData) {
        await Promise.resolve(this.tokenDataAdapter.setTokenData(tokensData));
        this.events.emit('tokenChange', tokensData);
        this.events.emit('tokensUpdated', tokensData);
    }
}

// This file was autogenerated from a .proto file, DO NOT EDIT!
var ErrorCode$1;
(function (ErrorCode) {
    ErrorCode["ERROR_CODE_UNSPECIFIED"] = "ERROR_CODE_UNSPECIFIED";
    ErrorCode["INVALID_SESSION"] = "INVALID_SESSION";
    ErrorCode["ACCESS_TOKEN_INVALID"] = "ACCESS_TOKEN_INVALID";
    ErrorCode["ACCESS_TOKEN_EXPIRED"] = "ACCESS_TOKEN_EXPIRED";
    ErrorCode["ACCESS_FORBIDDEN"] = "ACCESS_FORBIDDEN";
    ErrorCode["USER_NOT_FOUND"] = "USER_NOT_FOUND";
    ErrorCode["GRANT_REVOKED"] = "GRANT_REVOKED";
    ErrorCode["GRANT_NOT_FOUND"] = "GRANT_NOT_FOUND";
    ErrorCode["CLIENT_BINDING_MISMATCH"] = "CLIENT_BINDING_MISMATCH";
    ErrorCode["UPDATE_APP_TO_NEW_VERSION"] = "UPDATE_APP_TO_NEW_VERSION";
    ErrorCode["OTP_INVALID"] = "OTP_INVALID";
    ErrorCode["OTP_NOT_FOUND_OR_EXPIRED"] = "OTP_NOT_FOUND_OR_EXPIRED";
    ErrorCode["OTP_ATTEMPTS_EXCEEDED"] = "OTP_ATTEMPTS_EXCEEDED";
    ErrorCode["REQUEST_TOKEN_NOT_FOUND_OR_EXPIRED"] = "REQUEST_TOKEN_NOT_FOUND_OR_EXPIRED";
    ErrorCode["REQUEST_TOKEN_INVALID"] = "REQUEST_TOKEN_INVALID";
    ErrorCode["REFRESH_TOKEN_INVALID"] = "REFRESH_TOKEN_INVALID";
    ErrorCode["REFRESH_TOKEN_EXPIRED"] = "REFRESH_TOKEN_EXPIRED";
    ErrorCode["REFRESH_TOKEN_REPLAYED"] = "REFRESH_TOKEN_REPLAYED";
    ErrorCode["INVALID_INPUT"] = "INVALID_INPUT";
    ErrorCode["UNKNOWN_ERROR"] = "UNKNOWN_ERROR";
})(ErrorCode$1 || (ErrorCode$1 = {}));
const Nip44DecryptResponseValidate = (o, opts = {}, path = 'Nip44DecryptResponse::root.') => {
    if (opts.checkOptionalsAreSet && opts.allOptionalsAreSet)
        return new Error(path + ': only one of checkOptionalsAreSet or allOptionalNonDefault can be set for each message');
    if (typeof o !== 'object' || o === null)
        return new Error(path + ': object is not an instance of an object or is null');
    if (typeof o.plaintext !== 'string')
        return new Error(`${path}.plaintext: is not a string`);
    if (opts.plaintext_CustomCheck && !opts.plaintext_CustomCheck(o.plaintext))
        return new Error(`${path}.plaintext: custom check failed`);
    return null;
};
const Nip44EncryptResponseValidate = (o, opts = {}, path = 'Nip44EncryptResponse::root.') => {
    if (opts.checkOptionalsAreSet && opts.allOptionalsAreSet)
        return new Error(path + ': only one of checkOptionalsAreSet or allOptionalNonDefault can be set for each message');
    if (typeof o !== 'object' || o === null)
        return new Error(path + ': object is not an instance of an object or is null');
    if (typeof o.ciphertext !== 'string')
        return new Error(`${path}.ciphertext: is not a string`);
    if (opts.ciphertext_CustomCheck && !opts.ciphertext_CustomCheck(o.ciphertext))
        return new Error(`${path}.ciphertext: custom check failed`);
    return null;
};
const UserNostrPubKeyValidate = (o, opts = {}, path = 'UserNostrPubKey::root.') => {
    if (opts.checkOptionalsAreSet && opts.allOptionalsAreSet)
        return new Error(path + ': only one of checkOptionalsAreSet or allOptionalNonDefault can be set for each message');
    if (typeof o !== 'object' || o === null)
        return new Error(path + ': object is not an instance of an object or is null');
    if (typeof o.pubkey !== 'string')
        return new Error(`${path}.pubkey: is not a string`);
    if (opts.pubkey_CustomCheck && !opts.pubkey_CustomCheck(o.pubkey))
        return new Error(`${path}.pubkey: custom check failed`);
    return null;
};
const SocketAuthRequestTokenValidate = (o, opts = {}, path = 'SocketAuthRequestToken::root.') => {
    if (opts.checkOptionalsAreSet && opts.allOptionalsAreSet)
        return new Error(path + ': only one of checkOptionalsAreSet or allOptionalNonDefault can be set for each message');
    if (typeof o !== 'object' || o === null)
        return new Error(path + ': object is not an instance of an object or is null');
    if (typeof o.request_token !== 'string')
        return new Error(`${path}.request_token: is not a string`);
    if (opts.request_token_CustomCheck && !opts.request_token_CustomCheck(o.request_token))
        return new Error(`${path}.request_token: custom check failed`);
    if (typeof o.expires_at !== 'number')
        return new Error(`${path}.expires_at: is not a number`);
    if (opts.expires_at_CustomCheck && !opts.expires_at_CustomCheck(o.expires_at))
        return new Error(`${path}.expires_at: custom check failed`);
    return null;
};
const NostrRelaysValidate = (o, opts = {}, path = 'NostrRelays::root.') => {
    if (opts.checkOptionalsAreSet && opts.allOptionalsAreSet)
        return new Error(path + ': only one of checkOptionalsAreSet or allOptionalNonDefault can be set for each message');
    if (typeof o !== 'object' || o === null)
        return new Error(path + ': object is not an instance of an object or is null');
    if (typeof o.relays !== 'object' || o.relays === null)
        return new Error(`${path}.relays: is not an object or is null`);
    for (const key in o.relays) {
        const relaysErr = RelayPolicyValidate(o.relays[key], opts.relays_EntryOptions, `${path}.relays['${key}']`);
        if (relaysErr !== null)
            return relaysErr;
    }
    return null;
};
const NostrSignResponseValidate = (o, opts = {}, path = 'NostrSignResponse::root.') => {
    if (opts.checkOptionalsAreSet && opts.allOptionalsAreSet)
        return new Error(path + ': only one of checkOptionalsAreSet or allOptionalNonDefault can be set for each message');
    if (typeof o !== 'object' || o === null)
        return new Error(path + ': object is not an instance of an object or is null');
    if (typeof o.signedEvent !== 'string')
        return new Error(`${path}.signedEvent: is not a string`);
    if (opts.signedEvent_CustomCheck && !opts.signedEvent_CustomCheck(o.signedEvent))
        return new Error(`${path}.signedEvent: custom check failed`);
    return null;
};
const RelayPolicyValidate = (o, opts = {}, path = 'RelayPolicy::root.') => {
    if (opts.checkOptionalsAreSet && opts.allOptionalsAreSet)
        return new Error(path + ': only one of checkOptionalsAreSet or allOptionalNonDefault can be set for each message');
    if (typeof o !== 'object' || o === null)
        return new Error(path + ': object is not an instance of an object or is null');
    if (typeof o.read !== 'boolean')
        return new Error(`${path}.read: is not a boolean`);
    if (opts.read_CustomCheck && !opts.read_CustomCheck(o.read))
        return new Error(`${path}.read: custom check failed`);
    if (typeof o.write !== 'boolean')
        return new Error(`${path}.write: is not a boolean`);
    if (opts.write_CustomCheck && !opts.write_CustomCheck(o.write))
        return new Error(`${path}.write: custom check failed`);
    return null;
};
const Nip98EventResponseValidate = (o, opts = {}, path = 'Nip98EventResponse::root.') => {
    if (opts.checkOptionalsAreSet && opts.allOptionalsAreSet)
        return new Error(path + ': only one of checkOptionalsAreSet or allOptionalNonDefault can be set for each message');
    if (typeof o !== 'object' || o === null)
        return new Error(path + ': object is not an instance of an object or is null');
    if (typeof o.authorization_header !== 'string')
        return new Error(`${path}.authorization_header: is not a string`);
    if (opts.authorization_header_CustomCheck && !opts.authorization_header_CustomCheck(o.authorization_header))
        return new Error(`${path}.authorization_header: custom check failed`);
    return null;
};
const TokensDataValidate = (o, opts = {}, path = 'TokensData::root.') => {
    if (opts.checkOptionalsAreSet && opts.allOptionalsAreSet)
        return new Error(path + ': only one of checkOptionalsAreSet or allOptionalNonDefault can be set for each message');
    if (typeof o !== 'object' || o === null)
        return new Error(path + ': object is not an instance of an object or is null');
    if (typeof o.account_identifier !== 'string')
        return new Error(`${path}.account_identifier: is not a string`);
    if (opts.account_identifier_CustomCheck && !opts.account_identifier_CustomCheck(o.account_identifier))
        return new Error(`${path}.account_identifier: custom check failed`);
    if (typeof o.access_token !== 'string')
        return new Error(`${path}.access_token: is not a string`);
    if (opts.access_token_CustomCheck && !opts.access_token_CustomCheck(o.access_token))
        return new Error(`${path}.access_token: custom check failed`);
    if (typeof o.refresh_token !== 'string')
        return new Error(`${path}.refresh_token: is not a string`);
    if (opts.refresh_token_CustomCheck && !opts.refresh_token_CustomCheck(o.refresh_token))
        return new Error(`${path}.refresh_token: custom check failed`);
    if (typeof o.expires_at !== 'number')
        return new Error(`${path}.expires_at: is not a number`);
    if (opts.expires_at_CustomCheck && !opts.expires_at_CustomCheck(o.expires_at))
        return new Error(`${path}.expires_at: custom check failed`);
    if (typeof o.refresh_expires_at !== 'number')
        return new Error(`${path}.refresh_expires_at: is not a number`);
    if (opts.refresh_expires_at_CustomCheck && !opts.refresh_expires_at_CustomCheck(o.refresh_expires_at))
        return new Error(`${path}.refresh_expires_at: custom check failed`);
    if (typeof o.identifier !== 'string')
        return new Error(`${path}.identifier: is not a string`);
    if (opts.identifier_CustomCheck && !opts.identifier_CustomCheck(o.identifier))
        return new Error(`${path}.identifier: custom check failed`);
    return null;
};
const SocketErrorMessageValidate = (o, opts = {}, path = 'SocketErrorMessage::root.') => {
    if (opts.checkOptionalsAreSet && opts.allOptionalsAreSet)
        return new Error(path + ': only one of checkOptionalsAreSet or allOptionalNonDefault can be set for each message');
    if (typeof o !== 'object' || o === null)
        return new Error(path + ': object is not an instance of an object or is null');
    if (typeof o.error !== 'string')
        return new Error(`${path}.error: is not a string`);
    if (opts.error_CustomCheck && !opts.error_CustomCheck(o.error))
        return new Error(`${path}.error: custom check failed`);
    return null;
};

const ErrorCode = {
    ...ErrorCode$1,
    NOT_AUTHENTICATED: 'NOT_AUTHENTICATED',
    INVALID_RESPONSE: 'INVALID_RESPONSE',
    ALREADY_DESTROYED: 'ALREADY_DESTROYED',
    WIDGET_CONTAINER_NOT_FOUND: 'WIDGET_CONTAINER_NOT_FOUND'
};
class SanctumDKError extends Error {
    constructor(message, code, recoverable = true, cause) {
        super(message);
        this.name = 'SanctumDKError';
        this.code = code;
        this.recoverable = recoverable;
        this.cause = cause;
    }
}
const isReauthReason = (reason) => {
    return reason === ErrorCode.REFRESH_TOKEN_EXPIRED
        || reason === ErrorCode.REFRESH_TOKEN_INVALID
        || reason === ErrorCode.REFRESH_TOKEN_REPLAYED
        || reason === ErrorCode.GRANT_REVOKED;
};

// Mimics axios-like response structure
async function fetchWithAxiosStructure(url, options) {
    const response = await fetch(url, {
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers,
        },
    });
    if (!response.ok) {
        throw new Error(`HTTP error! status: ${response.status}`);
    }
    const data = await response.json();
    return { data };
}
// Create axios-like interface using fetch
const fetchAdapter = {
    get: async (url, config) => {
        return fetchWithAxiosStructure(url, {
            method: 'GET',
            headers: config?.headers,
        });
    },
    post: async (url, data, config) => {
        return fetchWithAxiosStructure(url, {
            method: 'POST',
            headers: config?.headers,
            body: JSON.stringify(data),
        });
    },
};

// This file was autogenerated from a .proto file, DO NOT EDIT!
var httpClient = (params) => ({
    GetNostrPubKey: async () => {
        const auth = await params.retrieveAccessTokenAuth();
        if (auth === null)
            throw new Error('retrieveAccessTokenAuth() returned null');
        let finalRoute = '/api/v2/user/pubkey';
        const { data } = await fetchAdapter.get(params.baseUrl + finalRoute, { headers: { 'authorization': auth } });
        if (data.status === 'ERROR' && typeof data.reason === 'string')
            return data;
        if (data.status === 'OK') {
            const result = data;
            if (!params.checkResult)
                return { status: 'OK', ...result };
            const error = UserNostrPubKeyValidate(result);
            if (error === null) {
                return { status: 'OK', ...result };
            }
            else
                return { status: 'ERROR', reason: error.message };
        }
        return { status: 'ERROR', reason: 'invalid response' };
    },
    GetNostrRelays: async () => {
        const auth = await params.retrieveAccessTokenAuth();
        if (auth === null)
            throw new Error('retrieveAccessTokenAuth() returned null');
        let finalRoute = '/api/v2/user/relays';
        const { data } = await fetchAdapter.get(params.baseUrl + finalRoute, { headers: { 'authorization': auth } });
        if (data.status === 'ERROR' && typeof data.reason === 'string')
            return data;
        if (data.status === 'OK') {
            const result = data;
            if (!params.checkResult)
                return { status: 'OK', ...result };
            const error = NostrRelaysValidate(result);
            if (error === null) {
                return { status: 'OK', ...result };
            }
            else
                return { status: 'ERROR', reason: error.message };
        }
        return { status: 'ERROR', reason: 'invalid response' };
    },
    SignNostrEvent: async (request) => {
        const auth = await params.retrieveAccessTokenAuth();
        if (auth === null)
            throw new Error('retrieveAccessTokenAuth() returned null');
        let finalRoute = '/api/v2/user/sign';
        const { data } = await fetchAdapter.post(params.baseUrl + finalRoute, request, { headers: { 'authorization': auth } });
        if (data.status === 'ERROR' && typeof data.reason === 'string')
            return data;
        if (data.status === 'OK') {
            const result = data;
            if (!params.checkResult)
                return { status: 'OK', ...result };
            const error = NostrSignResponseValidate(result);
            if (error === null) {
                return { status: 'OK', ...result };
            }
            else
                return { status: 'ERROR', reason: error.message };
        }
        return { status: 'ERROR', reason: 'invalid response' };
    },
    Nip44Decrypt: async (request) => {
        const auth = await params.retrieveAccessTokenAuth();
        if (auth === null)
            throw new Error('retrieveAccessTokenAuth() returned null');
        let finalRoute = '/api/v2/user/decrypt';
        const { data } = await fetchAdapter.post(params.baseUrl + finalRoute, request, { headers: { 'authorization': auth } });
        if (data.status === 'ERROR' && typeof data.reason === 'string')
            return data;
        if (data.status === 'OK') {
            const result = data;
            if (!params.checkResult)
                return { status: 'OK', ...result };
            const error = Nip44DecryptResponseValidate(result);
            if (error === null) {
                return { status: 'OK', ...result };
            }
            else
                return { status: 'ERROR', reason: error.message };
        }
        return { status: 'ERROR', reason: 'invalid response' };
    },
    Nip44Encrypt: async (request) => {
        const auth = await params.retrieveAccessTokenAuth();
        if (auth === null)
            throw new Error('retrieveAccessTokenAuth() returned null');
        let finalRoute = '/api/v2/user/encrypt';
        const { data } = await fetchAdapter.post(params.baseUrl + finalRoute, request, { headers: { 'authorization': auth } });
        if (data.status === 'ERROR' && typeof data.reason === 'string')
            return data;
        if (data.status === 'OK') {
            const result = data;
            if (!params.checkResult)
                return { status: 'OK', ...result };
            const error = Nip44EncryptResponseValidate(result);
            if (error === null) {
                return { status: 'OK', ...result };
            }
            else
                return { status: 'ERROR', reason: error.message };
        }
        return { status: 'ERROR', reason: 'invalid response' };
    },
    Nip98Event: async (request) => {
        const auth = await params.retrieveAccessTokenAuth();
        if (auth === null)
            throw new Error('retrieveAccessTokenAuth() returned null');
        let finalRoute = '/api/v2/user/nip98';
        const { data } = await fetchAdapter.post(params.baseUrl + finalRoute, request, { headers: { 'authorization': auth } });
        if (data.status === 'ERROR' && typeof data.reason === 'string')
            return data;
        if (data.status === 'OK') {
            const result = data;
            if (!params.checkResult)
                return { status: 'OK', ...result };
            const error = Nip98EventResponseValidate(result);
            if (error === null) {
                return { status: 'OK', ...result };
            }
            else
                return { status: 'ERROR', reason: error.message };
        }
        return { status: 'ERROR', reason: 'invalid response' };
    },
    MintFromRefreshToken: async (request) => {
        const auth = await params.retrieveGuestAuth();
        if (auth === null)
            throw new Error('retrieveGuestAuth() returned null');
        let finalRoute = '/api/guest/refresh';
        const { data } = await fetchAdapter.post(params.baseUrl + finalRoute, request, { headers: { 'authorization': auth } });
        if (data.status === 'ERROR' && typeof data.reason === 'string')
            return data;
        if (data.status === 'OK') {
            const result = data;
            if (!params.checkResult)
                return { status: 'OK', ...result };
            const error = TokensDataValidate(result);
            if (error === null) {
                return { status: 'OK', ...result };
            }
            else
                return { status: 'ERROR', reason: error.message };
        }
        return { status: 'ERROR', reason: 'invalid response' };
    },
});

const ACCESS_TOKEN_REFRESH_SKEW_MS = 60000;
class ApiFacade {
    constructor(deps) {
        this.deps = deps;
        this.refreshPromise = null;
        this.client = httpClient({
            baseUrl: deps.baseUrl,
            retrieveGuestAuth: async () => '',
            retrieveAccessTokenAuth: async () => {
                const token = await this.deps.session.getTokenData();
                return token ? `Bearer ${token.access_token}` : null;
            },
            retrieveLegacyAccessTokenAuth: async () => '',
            retrieveUserAuth: async () => '',
            encryptCallback: async () => {
                throw new Error('encryption callback not enabled');
            },
            decryptCallback: async () => {
                throw new Error('decryption callback not enabled');
            },
            deviceId: '',
        });
    }
    async requireAuth() {
        const tokenData = await this.deps.session.getTokenData();
        if (!tokenData) {
            throw new SanctumDKError('Not authenticated', ErrorCode.NOT_AUTHENTICATED, false);
        }
        if (Date.now() >= tokenData.refresh_expires_at) {
            return this.requireReauth(ErrorCode.REFRESH_TOKEN_EXPIRED);
        }
        if (Date.now() >= tokenData.expires_at - ACCESS_TOKEN_REFRESH_SKEW_MS) {
            return this.refreshTokensSingleFlight();
        }
        return tokenData;
    }
    async requireReauth(reason) {
        await this.deps.session.clear();
        this.deps.events.emit('reauthRequired', { reason });
        throw new SanctumDKError('Re-authentication required', reason, false);
    }
    isRefreshableAuthError(reason) {
        return reason === ErrorCode.ACCESS_TOKEN_EXPIRED || reason === ErrorCode.ACCESS_TOKEN_INVALID;
    }
    async refreshTokensSingleFlight() {
        if (this.refreshPromise) {
            return this.refreshPromise;
        }
        this.refreshPromise = (async () => {
            const current = await this.deps.session.getTokenData();
            if (!current) {
                return this.requireReauth(ErrorCode.REFRESH_TOKEN_INVALID);
            }
            const refresh = await this.client.MintFromRefreshToken({
                refresh_token: current.refresh_token,
            });
            if (refresh.status === 'ERROR') {
                if (isReauthReason(refresh.reason)) {
                    return this.requireReauth(refresh.reason);
                }
                throw new SanctumDKError(refresh.reason, refresh.reason);
            }
            await this.deps.session.updateTokensAfterRefresh(refresh);
            return refresh;
        })();
        try {
            return await this.refreshPromise;
        }
        finally {
            this.refreshPromise = null;
        }
    }
    async unwrapResult(promise) {
        const result = await promise;
        if (result.status === 'OK') {
            const { status: _status, ...payload } = await result;
            return payload;
        }
        throw new SanctumDKError(result.reason, result.reason);
    }
    async withRefreshRetry(fn) {
        try {
            return await this.unwrapResult(fn());
        }
        catch (error) {
            if (!(error instanceof SanctumDKError)) {
                throw error;
            }
            if (error.code === ErrorCode.GRANT_REVOKED) {
                return this.requireReauth(ErrorCode.GRANT_REVOKED);
            }
            if (!this.isRefreshableAuthError(error.code)) {
                throw error;
            }
            await this.refreshTokensSingleFlight();
            return this.unwrapResult(fn());
        }
    }
    async getPublicKey() {
        await this.requireAuth();
        const result = await this.withRefreshRetry(() => this.client.GetNostrPubKey());
        return result.pubkey;
    }
    async getRelays() {
        await this.requireAuth();
        const result = await this.withRefreshRetry(() => this.client.GetNostrRelays());
        return result.relays;
    }
    async signEvent(event) {
        await this.requireAuth();
        const result = await this.withRefreshRetry(() => this.client.SignNostrEvent({ usignedEvent: event }));
        return result.signedEvent;
    }
    async encrypt(plaintext, pubkey) {
        await this.requireAuth();
        const result = await this.withRefreshRetry(() => this.client.Nip44Encrypt({ plaintext, pubkey }));
        return result.ciphertext;
    }
    async decrypt(ciphertext, pubkey) {
        await this.requireAuth();
        const result = await this.withRefreshRetry(() => this.client.Nip44Decrypt({ ciphertext, pubkey }));
        return result.plaintext;
    }
    async createAuthHeader(url, method, body) {
        await this.requireAuth();
        const result = await this.withRefreshRetry(() => this.client.Nip98Event({
            url,
            method: method.toUpperCase(),
            request_body: body
        }));
        return result.authorization_header;
    }
}

const AUTH_SOCKET_CANCELLED_MESSAGE = 'cancelled by user';
const DEFAULT_CONNECT_TIMEOUT_MS = 10000;
const DEFAULT_MAX_RECONNECT_ATTEMPTS = 5;
const DEFAULT_RECONNECT_BACKOFF_MS = [10000, 10000, 10000, 20000, 20000, 30000, 60000];
const SOCKET_PROTOCOL_VERSION = 1;
class AuthSocketClient {
    constructor(options) {
        this.socket = null;
        this.resolveStart = null;
        this.rejectStart = null;
        this.connectTimeoutTimer = null;
        this.reconnectTimer = null;
        this.reconnectAttempts = 0;
        this.aborted = false;
        /** Last request_token from the server; sent on hello after reconnect so the backend can resume. */
        this.lastRequestToken = null;
        this.url = options.url;
        this.getClientKey = options.getClientKey;
        this.onState = options.onState;
        this.onRequestToken = options.onRequestToken;
        this.onError = options.onError;
        this.connectTimeoutMs = options.connectTimeoutMs ?? DEFAULT_CONNECT_TIMEOUT_MS;
        this.maxReconnectAttempts = options.maxReconnectAttempts ?? DEFAULT_MAX_RECONNECT_ATTEMPTS;
        this.reconnectBackoff = options.reconnectBackoff ?? [...DEFAULT_RECONNECT_BACKOFF_MS];
    }
    start() {
        if (this.socketPromise)
            return this.socketPromise;
        this.aborted = false;
        this.reconnectAttempts = 0;
        this.lastRequestToken = null;
        this.socketPromise = new Promise((resolve, reject) => {
            this.resolveStart = resolve;
            this.rejectStart = reject;
            this.openSocket();
        }).finally(() => {
            this.socketPromise = undefined;
            this.resolveStart = null;
            this.rejectStart = null;
            this.clearTimers();
        });
        return this.socketPromise;
    }
    abort() {
        if (this.aborted)
            return;
        this.aborted = true;
        this.lastRequestToken = null;
        this.clearTimers();
        this.cleanupSocket();
        const reject = this.rejectStart;
        this.rejectStart = null;
        this.resolveStart = null;
        reject?.(new Error(AUTH_SOCKET_CANCELLED_MESSAGE));
    }
    openSocket() {
        if (this.aborted || !this.resolveStart)
            return;
        this.clearConnectTimeout();
        this.cleanupSocket();
        this.onState?.(this.reconnectAttempts > 0 ? 'reconnecting' : 'connecting');
        let ws;
        try {
            ws = new WebSocket(this.url);
        }
        catch {
            this.terminalReject(new Error('Failed to create WebSocket'));
            return;
        }
        this.socket = ws;
        this.connectTimeoutTimer = window.setTimeout(() => {
            if (this.aborted)
                return;
            this.onError?.('Connection timed out');
            try {
                ws.close();
            }
            catch {
                /* noop */
            }
        }, this.connectTimeoutMs);
        ws.onopen = () => {
            if (this.aborted)
                return;
            this.clearConnectTimeout();
            this.reconnectAttempts = 0;
            this.onState?.('connected');
            void this.sendClientHello();
        };
        ws.onmessage = (event) => {
            if (this.aborted)
                return;
            const parsed = this.parseMessage(event.data);
            if (!parsed) {
                this.terminalReject(new Error('Failed to parse websocket response'));
                return;
            }
            if ('request_token' in parsed) {
                this.lastRequestToken = parsed.request_token;
                this.onRequestToken?.(parsed);
                return;
            }
            if ('access_token' in parsed && 'refresh_token' in parsed) {
                this.lastRequestToken = null;
                const resolve = this.resolveStart;
                this.cleanupSocket();
                resolve?.(parsed);
                return;
            }
            if ('error' in parsed) {
                this.terminalReject(new Error(parsed.error || 'Socket auth failed'));
            }
        };
        ws.onerror = () => { };
        ws.onclose = () => {
            this.clearConnectTimeout();
            if (this.aborted)
                return;
            if (this.reconnectAttempts >= this.maxReconnectAttempts) {
                this.terminalReject(new Error('WebSocket reconnection limit reached'));
                return;
            }
            const idx = Math.min(this.reconnectAttempts, this.reconnectBackoff.length - 1);
            const delay = this.reconnectBackoff[idx] ?? this.reconnectBackoff[this.reconnectBackoff.length - 1];
            this.reconnectAttempts += 1;
            this.reconnectTimer = window.setTimeout(() => {
                this.reconnectTimer = null;
                this.openSocket();
            }, delay);
        };
    }
    terminalReject(err) {
        this.onError?.(err.message);
        const reject = this.rejectStart;
        this.rejectStart = null;
        this.resolveStart = null;
        this.cleanupSocket();
        reject?.(err);
    }
    async sendClientHello() {
        if (!this.socket || this.aborted)
            return;
        try {
            const hello = {
                client_key: await this.getClientKey(),
                protocol_version: SOCKET_PROTOCOL_VERSION,
                request_token: this.lastRequestToken ?? undefined,
            };
            this.socket.send(JSON.stringify(hello));
        }
        catch {
            this.terminalReject(new Error('Failed to initialize websocket auth'));
        }
    }
    parseMessage(raw) {
        if (typeof raw !== 'string')
            return null;
        let parsedMessage;
        try {
            parsedMessage = JSON.parse(raw);
        }
        catch {
            return null;
        }
        if (SocketAuthRequestTokenValidate(parsedMessage) === null) {
            return parsedMessage;
        }
        if (TokensDataValidate(parsedMessage) === null) {
            return parsedMessage;
        }
        if (SocketErrorMessageValidate(parsedMessage) === null) {
            return parsedMessage;
        }
        return null;
    }
    clearConnectTimeout() {
        if (this.connectTimeoutTimer !== null) {
            clearTimeout(this.connectTimeoutTimer);
            this.connectTimeoutTimer = null;
        }
    }
    clearTimers() {
        this.clearConnectTimeout();
        if (this.reconnectTimer !== null) {
            clearTimeout(this.reconnectTimer);
            this.reconnectTimer = null;
        }
    }
    cleanupSocket() {
        if (!this.socket)
            return;
        const ws = this.socket;
        this.socket = null;
        ws.onopen = null;
        ws.onclose = null;
        ws.onerror = null;
        ws.onmessage = null;
        try {
            if (ws.readyState === WebSocket.CONNECTING || ws.readyState === WebSocket.OPEN) {
                ws.close();
            }
        }
        catch {
            /* noop */
        }
    }
}

const hourglassIcon = `<svg xmlns="http://www.w3.org/2000/svg" class="ionicon" viewBox="0 0 512 512"><path fill="currentColor" d="M415.7 427.13c-8.74-76.89-43.83-108.76-69.46-132C328.52 279 320 270.61 320 256c0-14.41 8.49-22.64 26.16-38.44 25.93-23.17 61.44-54.91 69.56-132.84a47 47 0 00-12-36.26A50.3 50.3 0 00366.39 32H145.61a50.34 50.34 0 00-37.39 16.46 47.05 47.05 0 00-11.94 36.26c8.09 77.68 43.47 109.19 69.3 132.19C183.42 232.8 192 241.09 192 256c0 15.1-8.6 23.56-26.5 39.75-25.5 23.1-60.5 54.73-69.2 131.38a46.59 46.59 0 0011.7 36.2A50.44 50.44 0 00145.61 480h220.78A50.44 50.44 0 00404 463.33a46.59 46.59 0 0011.7-36.2zM343.3 432H169.13c-15.6 0-20-18-9.06-29.16C186.55 376 240 356.78 240 326V224c0-19.85-38-35-61.51-67.2-3.88-5.31-3.49-12.8 6.37-12.8h142.73c8.41 0 10.22 7.43 6.4 12.75C310.82 189 272 204.05 272 224v102c0 30.53 55.71 47 80.4 76.87 9.95 12.04 6.47 29.13-9.1 29.13z"/></svg>`;
const settingsIcon = `<svg xmlns="http://www.w3.org/2000/svg" class="ionicon" viewBox="0 0 512 512"><path d="M262.29 192.31a64 64 0 1057.4 57.4 64.13 64.13 0 00-57.4-57.4zM416.39 256a154.34 154.34 0 01-1.53 20.79l45.21 35.46a10.81 10.81 0 012.45 13.75l-42.77 74a10.81 10.81 0 01-13.14 4.59l-44.9-18.08a16.11 16.11 0 00-15.17 1.75A164.48 164.48 0 01325 400.8a15.94 15.94 0 00-8.82 12.14l-6.73 47.89a11.08 11.08 0 01-10.68 9.17h-85.54a11.11 11.11 0 01-10.69-8.87l-6.72-47.82a16.07 16.07 0 00-9-12.22 155.3 155.3 0 01-21.46-12.57 16 16 0 00-15.11-1.71l-44.89 18.07a10.81 10.81 0 01-13.14-4.58l-42.77-74a10.8 10.8 0 012.45-13.75l38.21-30a16.05 16.05 0 006-14.08c-.36-4.17-.58-8.33-.58-12.5s.21-8.27.58-12.35a16 16 0 00-6.07-13.94l-38.19-30A10.81 10.81 0 0149.48 186l42.77-74a10.81 10.81 0 0113.14-4.59l44.9 18.08a16.11 16.11 0 0015.17-1.75A164.48 164.48 0 01187 111.2a15.94 15.94 0 008.82-12.14l6.73-47.89A11.08 11.08 0 01213.23 42h85.54a11.11 11.11 0 0110.69 8.87l6.72 47.82a16.07 16.07 0 009 12.22 155.3 155.3 0 0121.46 12.57 16 16 0 0015.11 1.71l44.89-18.07a10.81 10.81 0 0113.14 4.58l42.77 74a10.8 10.8 0 01-2.45 13.75l-38.21 30a16.05 16.05 0 00-6.05 14.08c.33 4.14.55 8.3.55 12.47z" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="32"/></svg>`;
const logoutIcon = `<svg xmlns="http://www.w3.org/2000/svg" class="ionicon" viewBox="0 0 512 512"><path fill="currentColor" d="M160 256a16 16 0 0116-16h144V136c0-32-33.79-56-64-56H104a56.06 56.06 0 00-56 56v240a56.06 56.06 0 0056 56h160a56.06 56.06 0 0056-56V272H176a16 16 0 01-16-16zM459.31 244.69l-80-80a16 16 0 00-22.62 22.62L409.37 240H320v32h89.37l-52.68 52.69a16 16 0 1022.62 22.62l80-80a16 16 0 000-22.62z"/></svg>`;
const checkMarkIcon = `<svg xmlns="http://www.w3.org/2000/svg" class="ionicon" viewBox="0 0 512 512"><path fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="32" d="M416 128L192 384l-96-96"/></svg>`;

var img$1 = "data:image/svg+xml,%3csvg width='454' height='100' viewBox='0 0 454 100' fill='none' xmlns='http://www.w3.org/2000/svg'%3e %3cpath d='M60.9561 43.9736L42.6572 46.9736L55.6484 23.7578C63.1919 29.994 67.9999 39.4218 68 49.9736C68 68.7513 52.7777 83.9736 34 83.9736C23.4358 83.9736 11.1246 89.9288 0.0400391 99.8965L60.9561 43.9736Z' fill='%2325E673'/%3e%3cpath d='M13.1396 76.8232C5.14412 70.6022 0 60.8889 0 49.9736C0.0001297 31.1961 15.2224 15.9736 34 15.9736C45.1929 15.9736 57.9504 10 68.9004 0C68.9004 0 68.8822 0.027626 68.8447 0.0791016L8.95605 55.9736L26.7207 52.9736L13.1396 76.8232Z' fill='%2325E673'/%3e %3cpath d='M383.9 71.688L399.132 29H415.004L419.228 48.2L437.212 29H453.084L437.852 71.688H424.604L432.476 49.608L411.676 71.688H410.076L405.02 49.608L397.148 71.688H383.9Z' fill='%23F1F1F1'/%3e %3cpath d='M369.679 60.168L380.751 29H393.999L381.391 64.264C380.495 66.568 379.023 68.424 376.911 69.704C374.799 71.048 372.559 71.688 370.191 71.688H342.351C341.199 71.688 340.111 71.56 339.215 71.176C338.319 70.856 337.551 70.344 337.039 69.704C336.463 69.064 336.079 68.296 335.951 67.4C335.823 66.504 335.951 65.416 336.399 64.264L349.007 29H362.255L351.183 60.168H369.679Z' fill='%23F1F1F1'/%3e %3cpath d='M336.9 40.5195H325.875L314.739 71.6875H301.536L312.627 40.5195H299.208C299.848 40.5195 300.424 40.3278 301.064 39.8799C301.64 39.4319 302.217 38.8554 302.729 38.1514C303.24 37.5114 303.688 36.7435 304.072 35.9756C304.456 35.2077 304.841 34.4398 305.097 33.6719L306.761 29H341L336.9 40.5195Z' fill='%23F1F1F1'/%3e %3cpath d='M250.914 71.688C249.762 71.688 248.674 71.56 247.778 71.176C246.882 70.856 246.114 70.344 245.602 69.704C245.026 69.064 244.642 68.296 244.514 67.4C244.386 66.504 244.514 65.416 244.962 64.264L254.946 36.424C255.778 34.12 257.25 32.328 259.362 30.984C261.474 29.704 263.714 29 266.146 29H297.826L296.162 33.672C295.906 34.44 295.522 35.208 295.138 35.976C294.754 36.744 294.306 37.512 293.794 38.152C293.282 38.856 292.706 39.432 292.13 39.88C291.49 40.328 290.914 40.52 290.274 40.52H266.722L259.746 60.168H286.754L282.594 71.688H250.914Z' fill='%23F1F1F1'/%3e %3cpath d='M203.419 37.256L200.923 29H220.443L230.491 52.04H230.939L239.195 29H252.443L237.211 71.688H223.067L213.019 48.712H212.571L204.379 71.688H191.131L203.419 37.256Z' fill='%23F1F1F1'/%3e %3cpath d='M159.078 37.256L156.582 29H183.526L186.854 71.688H172.198L171.814 65.672H151.206L146.47 71.688H131.878L159.078 37.256ZM170.214 41.416L160.23 54.088H170.982L170.214 41.416Z' fill='%23F1F1F1'/%3e %3cpath d='M99.0444 56.136C97.8924 56.136 96.8044 56.008 95.9084 55.624C94.9484 55.304 94.1804 54.792 93.6684 54.152C93.0924 53.512 92.7724 52.744 92.6444 51.848C92.5164 50.952 92.6444 49.864 93.0924 48.712L97.5084 36.424C98.3404 34.12 99.8124 32.328 101.924 30.984C104.036 29.704 106.276 29 108.708 29H143.076L141.412 33.672C141.156 34.44 140.772 35.208 140.388 35.976C140.004 36.744 139.556 37.512 139.044 38.152C138.532 38.856 137.956 39.432 137.38 39.88C136.74 40.328 136.164 40.52 135.524 40.52H109.284L107.812 44.616H128.932C131.236 44.616 133.028 45.32 134.244 46.6C135.46 47.944 135.652 49.736 134.82 52.04L130.468 64.264C129.572 66.568 128.1 68.424 125.988 69.704C123.876 71.048 121.636 71.688 119.268 71.688H84.9004L89.0604 60.168H118.756L120.164 56.136H99.0444Z' fill='%23F1F1F1'/%3e%3c/svg%3e";

var img = "data:image/svg+xml,%3csvg width='454' height='100' viewBox='0 0 454 100' fill='none' xmlns='http://www.w3.org/2000/svg'%3e %3cpath d='M60.9561 43.9736L42.6572 46.9736L55.6484 23.7578C63.1919 29.994 67.9999 39.4218 68 49.9736C68 68.7513 52.7777 83.9736 34 83.9736C23.4358 83.9736 11.1246 89.9288 0.0400391 99.8965L60.9561 43.9736Z' fill='%234FD27A'/%3e%3cpath d='M13.1396 76.8232C5.14412 70.6022 0 60.8889 0 49.9736C0.0001297 31.1961 15.2224 15.9736 34 15.9736C45.1929 15.9736 57.9504 10 68.9004 0C68.9004 0 68.8822 0.027626 68.8447 0.0791016L8.95605 55.9736L26.7207 52.9736L13.1396 76.8232Z' fill='%234FD27A'/%3e %3cpath d='M383.9 71.688L399.132 29H415.004L419.228 48.2L437.212 29H453.084L437.852 71.688H424.604L432.476 49.608L411.676 71.688H410.076L405.02 49.608L397.148 71.688H383.9Z' fill='%23111213'/%3e %3cpath d='M369.679 60.168L380.751 29H393.999L381.391 64.264C380.495 66.568 379.023 68.424 376.911 69.704C374.799 71.048 372.559 71.688 370.191 71.688H342.351C341.199 71.688 340.111 71.56 339.215 71.176C338.319 70.856 337.551 70.344 337.039 69.704C336.463 69.064 336.079 68.296 335.951 67.4C335.823 66.504 335.951 65.416 336.399 64.264L349.007 29H362.255L351.183 60.168H369.679Z' fill='%23111213'/%3e %3cpath d='M336.9 40.5195H325.875L314.739 71.6875H301.536L312.627 40.5195H299.208C299.848 40.5195 300.424 40.3278 301.064 39.8799C301.64 39.4319 302.217 38.8554 302.729 38.1514C303.24 37.5114 303.688 36.7435 304.072 35.9756C304.456 35.2077 304.841 34.4398 305.097 33.6719L306.761 29H341L336.9 40.5195Z' fill='%23111213'/%3e %3cpath d='M250.914 71.688C249.762 71.688 248.674 71.56 247.778 71.176C246.882 70.856 246.114 70.344 245.602 69.704C245.026 69.064 244.642 68.296 244.514 67.4C244.386 66.504 244.514 65.416 244.962 64.264L254.946 36.424C255.778 34.12 257.25 32.328 259.362 30.984C261.474 29.704 263.714 29 266.146 29H297.826L296.162 33.672C295.906 34.44 295.522 35.208 295.138 35.976C294.754 36.744 294.306 37.512 293.794 38.152C293.282 38.856 292.706 39.432 292.13 39.88C291.49 40.328 290.914 40.52 290.274 40.52H266.722L259.746 60.168H286.754L282.594 71.688H250.914Z' fill='%23111213'/%3e %3cpath d='M203.419 37.256L200.923 29H220.443L230.491 52.04H230.939L239.195 29H252.443L237.211 71.688H223.067L213.019 48.712H212.571L204.379 71.688H191.131L203.419 37.256Z' fill='%23111213'/%3e %3cpath d='M159.078 37.256L156.582 29H183.526L186.854 71.688H172.198L171.814 65.672H151.206L146.47 71.688H131.878L159.078 37.256ZM170.214 41.416L160.23 54.088H170.982L170.214 41.416Z' fill='%23111213'/%3e %3cpath d='M99.0444 56.136C97.8924 56.136 96.8044 56.008 95.9084 55.624C94.9484 55.304 94.1804 54.792 93.6684 54.152C93.0924 53.512 92.7724 52.744 92.6444 51.848C92.5164 50.952 92.6444 49.864 93.0924 48.712L97.5084 36.424C98.3404 34.12 99.8124 32.328 101.924 30.984C104.036 29.704 106.276 29 108.708 29H143.076L141.412 33.672C141.156 34.44 140.772 35.208 140.388 35.976C140.004 36.744 139.556 37.512 139.044 38.152C138.532 38.856 137.956 39.432 137.38 39.88C136.74 40.328 136.164 40.52 135.524 40.52H109.284L107.812 44.616H128.932C131.236 44.616 133.028 45.32 134.244 46.6C135.46 47.944 135.652 49.736 134.82 52.04L130.468 64.264C129.572 66.568 128.1 68.424 125.988 69.704C123.876 71.048 121.636 71.688 119.268 71.688H84.9004L89.0604 60.168H118.756L120.164 56.136H99.0444Z' fill='%23111213'/%3e%3c/svg%3e";

var shocknetGlobal = "/*\n * ═══════════════════════════════════════════════════════════════════════════\n *  SHOCKNET — Design System  ·  Global Stylesheet\n *  shocknet-global.css  ·  v1.2\n * ═══════════════════════════════════════════════════════════════════════════\n *\n *  This file defines every rule shared across ALL SHOCKNET products.\n *  Import it once, before any product-specific stylesheet.\n *\n *  LOAD ORDER\n *  ─────────────────────────────────────────────────────────────────────────\n *  1. shocknet-global.css          ← this file (always required)\n *  2. shocknet-[product].css       ← one per product (optional per page)\n *\n *  THEME SWITCHING\n *  ─────────────────────────────────────────────────────────────────────────\n *  Default: dark.\n *  Switch:  document.documentElement.setAttribute('data-theme', 'light')\n *           document.documentElement.setAttribute('data-theme', 'dark')\n *\n *  TYPEFACES  — load BEFORE this file\n *  ─────────────────────────────────────────────────────────────────────────\n *  DM Mono   weights 300 · 400 · 500   fonts.google.com/specimen/DM+Mono\n *  Inter     weights 400 · 500 · 600   rsms.me/inter\n *\n *  CDN snippet (development — self-host in production for GDPR compliance):\n *  <link href=\"https://fonts.googleapis.com/css2?family=DM+Mono:ital,wght@0,300;0,400;0,500;1,300;1,400&family=Inter:wght@400;500;600&display=swap\" rel=\"stylesheet\">\n *\n * ═══════════════════════════════════════════════════════════════════════════\n *  TABLE OF CONTENTS\n *  ─────────────────────────────────────────────────────────────────────────\n *  01  Core Color Tokens          :root · dark theme defaults\n *  02  Light Theme Override       [data-theme=\"light\"]\n *  03  Typography Tokens          font stacks, sizes, weights, spacing\n *  04  Spacing Scale              4 px base unit\n *  05  Border Radius\n *  06  Transitions\n *  07  Elevation & Glow\n *  08  Grid & Layout\n *  09  Global Reset & Base\n *  10  Typography Elements        h1–h6, p, code, .section-tag, .rule, hr\n *  11  Surfaces                   .card, .card-sm, .card-inset, .demo-box\n *  12  Navigation                 .nav, .nav-links, .nav-link, .toggle-btn\n *  13  Buttons — Base             .btn, .btn-sm, ghost variants\n *  14  Buttons — SHOCKNET\n *  15  Pills                      .pill + neutral variants\n *  16  Download Row               .dl-row, .dl-btn\n *  17  Spec Row                   .spec-row\n *  18  Callout                    .callout\n *  19  Logo Box & Mark Pair       .logo-box, .mark-pair, .mark-dark, .mark-light\n *  20  Color Swatches             .swatch, .swatch-split\n *  21  Chapter / Section Labels   .chapter, .section-tag, .product-bar\n *  22  Icon Cells                 .icon-cell, .icon-grid\n *  23  App Icons & Favicons       .app-icon, .fav-box\n *  24  Form Elements              input, textarea, select\n *  25  Theme Toggle               .toggle-wrap, .toggle-btn\n *  26  Do / Don't Grid            .do-dont-grid, .dont-cell\n *  27  Voice & Tone               .voice-pillars, .tone-matrix, .copy-example\n *  28  Co-branding                .cobranding-rule, .excl-zone\n *  29  SVG Helpers                .wm, .cover-grid\n *  30  Footer\n *  31  Utilities\n *  32  Responsive Breakpoints\n * ═══════════════════════════════════════════════════════════════════════════\n */\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  01  CORE COLOR TOKENS  —  :root  (dark theme is the default)\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n:root,\n[data-theme=\"dark\"] {\n\n  /* ── Background & Surface ──────────────────────────────────────────── */\n  --color-bg:         #111213;   /* Night    */\n  --color-surface:    #212325;   /* Coal     */\n  --color-surface-2:  #1A1B1C;\n  --color-surface-3:  #2C2E30;\n\n  /* ── Border ─────────────────────────────────────────────────────────── */\n  --color-border:     #3C3C3C;   /* Graphene */\n  --color-border-sub: #1E1E1E;\n\n  /* ── Text ───────────────────────────────────────────────────────────── */\n  --color-text:       #F1F1F1;   /* Ice      */\n  --color-text-muted: #8A8A8A;   /* Dust     */\n  --color-text-dim:   #505050;\n\n  /* ── Nav ─────────────────────────────────────────────────────────────── */\n  --color-nav-bg:     rgba(17, 18, 19, 0.92);\n  --color-nav-border: #3C3C3C;\n\n  /* ── Ghost elements ─────────────────────────────────────────────────── */\n  --color-ghost-border: #3C3C3C;\n  --color-ghost-text:   #F1F1F1;\n\n  /* ── Grid overlay ───────────────────────────────────────────────────── */\n  --color-grid-line:  rgba(255, 255, 255, 0.025);\n\n  /* ── SHOCKNET parent accent ─────────────────────────────────────────── */\n  --color-sn:         #F1F1F1;\n  --color-sn-on:      #111213;\n\n  /* ── Semantic status ────────────────────────────────────────────────── */\n  --color-danger:  #FF0050;\n  --color-success: #25E673;\n  --color-warning: #F7FF00;\n  --color-info:    #1BCFE0;\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  02  LIGHT THEME OVERRIDE\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n[data-theme=\"light\"] {\n\n  --color-bg:         #F2F2EF;   /* Shell    */\n  --color-surface:    #FFFFFF;\n  --color-surface-2:  #F7F7F5;\n  --color-surface-3:  #EBEBEA;\n\n  --color-border:     #D0D0CC;\n  --color-border-sub: #E8E8E5;\n\n  --color-text:       #111213;\n  --color-text-muted: #555555;\n  --color-text-dim:   #999999;\n\n  --color-nav-bg:     rgba(242, 242, 239, 0.94);\n  --color-nav-border: #D0D0CC;\n\n  --color-ghost-border: #D0D0CC;\n  --color-ghost-text:   #111213;\n\n  --color-grid-line:  rgba(0, 0, 0, 0.04);\n\n  --color-sn:         #111213;\n  --color-sn-on:      #F2F2EF;\n\n  --color-danger:  #FF3366;\n  --color-success: #4FD27A;\n  --color-warning: #F0E91A;\n  --color-info:    #4FD6F0;\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  03  TYPOGRAPHY TOKENS\n *\n *  Heading font: DM Mono\n *    H1–H3  weight 300 (Light)\n *    H4–H6  weight 400 (Regular)\n *\n *  Letter-spacing per level (exact values from brand manual):\n *    H1  −0.042em   H2  −0.021em   H3  −0.014em\n *    H4  −0.007em   H5  +0.007em   H6  +0.042em + ALLCAPS\n *\n *  Body / buttons: Inter\n *  Labels / mono:  DM Mono\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n:root {\n  /* ── Font Families ───────────────────────────────────────────────────── */\n  --font-display: 'DM Mono', 'Courier New', monospace;\n  --font-body:    'Inter', system-ui, -apple-system, sans-serif;\n  --font-mono:    'DM Mono', 'Liberation Mono', 'Courier New', monospace;\n\n  /* ── Font Sizes ──────────────────────────────────────────────────────── */\n  --text-h1:    42px;\n  --text-h2:    36px;\n  --text-h3:    24px;\n  --text-h4:    21px;\n  --text-h5:    18px;\n  --text-h6:    16px;\n  --text-base:  16px;   /* Body copy, button labels                       */\n  --text-sm:    14px;   /* Small body, captions                           */\n  --text-xs:    12px;   /* Mono support / technical                       */\n  --text-label: 10px;   /* Pill labels, nav links, section tags           */\n  --text-micro:  8px;   /* Annotations, badges                            */\n\n  /* ── Font Weights ────────────────────────────────────────────────────── */\n  --weight-light:    300;   /* DM Mono — H1, H2, H3                       */\n  --weight-regular:  400;   /* DM Mono — H4, H5, H6 · Inter body          */\n  --weight-medium:   500;   /* Inter — buttons, emphasis                  */\n  --weight-semibold: 600;   /* Inter — strong emphasis (use sparingly)    */\n\n  /* ── Letter Spacing — Headings (exact per level, do not override) ────── */\n  --tracking-h1: -0.042em;\n  --tracking-h2: -0.021em;\n  --tracking-h3: -0.014em;\n  --tracking-h4: -0.007em;\n  --tracking-h5:  0.007em;\n  --tracking-h6:  0.042em;  /* H6 also uses text-transform: uppercase     */\n\n  /* ── Letter Spacing — Interface ─────────────────────────────────────── */\n  --tracking-label: 0.15em;   /* Nav links, section tags, pills           */\n  --tracking-mono:  0.05em;   /* DM Mono body / technical                 */\n  --tracking-wide:  0.20em;   /* Section eyebrows                         */\n\n  /* ── Line Heights ────────────────────────────────────────────────────── */\n  --leading-display: 1.10;\n  --leading-heading: 1.15;\n  --leading-body:    1.75;\n  --leading-tight:   1.30;\n  --leading-mono:    1.60;\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  04  SPACING SCALE  (4 px base unit — all values are multiples of 4)\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n:root {\n  --space-1:   4px;\n  --space-2:   8px;\n  --space-3:  12px;\n  --space-4:  16px;\n  --space-5:  24px;\n  --space-6:  32px;\n  --space-7:  48px;\n  --space-8:  64px;\n  --space-9:  96px;\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  05  BORDER RADIUS\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n:root {\n  --radius-sm:   5px;\n  --radius-md:  10px;\n  --radius-lg:  16px;\n  --radius-xl:  24px;\n  --radius-full: 9999px;\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  06  TRANSITIONS\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n:root {\n  --transition-fast:   .15s ease;\n  --transition-base:   .25s ease;\n  --transition-slow:   .40s ease;\n  --transition-theme:  .35s ease;\n  --transition-spring: .40s cubic-bezier(.34, 1.56, .64, 1);\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  07  ELEVATION & GLOW\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n:root {\n  --shadow-sm:  0 1px  3px rgba(0,0,0,.35), 0 1px 2px rgba(0,0,0,.20);\n  --shadow-md:  0 4px 12px rgba(0,0,0,.40), 0 2px 6px rgba(0,0,0,.25);\n  --shadow-lg:  0 12px 32px rgba(0,0,0,.50), 0 4px 12px rgba(0,0,0,.30);\n\n  /* Product glow — used on button hover, active pill, focus ring */\n  --glow-sn: 0 0 16px rgba(241,241,241,.18);\n  --glow-sw: 0 0 20px rgba(27,207,224,.22);\n  --glow-lp: 0 0 20px rgba(255,138,0,.22);\n  --glow-lv: 0 0 20px rgba(255,0,80,.22);\n  --glow-cl: 0 0 20px rgba(247,255,0,.18);\n  --glow-sa: 0 0 20px rgba(37,230,115,.20);\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  08  GRID & LAYOUT\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n:root {\n  --grid-columns: 12;\n  --grid-gutter:  20px;\n  --grid-max-w:   1200px;\n  --page-pad:      48px;\n  --page-pad-md:   24px;\n  --page-pad-sm:   16px;\n}\n\n.page {\n  max-width: var(--grid-max-w);\n  margin-inline: auto;\n  padding-inline: var(--page-pad);\n}\n\n.g2 { display: grid; grid-template-columns: 1fr 1fr;       gap: 20px; }\n.g3 { display: grid; grid-template-columns: 1fr 1fr 1fr;   gap: 18px; }\n.g4 { display: grid; grid-template-columns: 1fr 1fr 1fr 1fr; gap: 14px; }\n\n.grid-12 { display: grid; grid-template-columns: repeat(12, 1fr); gap: var(--grid-gutter); }\n.col-12  { grid-column: span 12; }\n.col-8   { grid-column: span  8; }\n.col-6   { grid-column: span  6; }\n.col-4   { grid-column: span  4; }\n.col-3   { grid-column: span  3; }\n\n.row { display: flex; align-items: center; gap: 12px; flex-wrap: wrap; }\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  09  GLOBAL RESET & BASE\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n*,\n*::before,\n*::after { box-sizing: border-box; margin: 0; padding: 0; }\n\nhtml { scroll-behavior: smooth; -webkit-text-size-adjust: 100%; }\n\nbody {\n  background-color: var(--color-bg);\n  color:            var(--color-text);\n  font-family:      var(--font-body);\n  font-size:        var(--text-base);\n  line-height:      var(--leading-body);\n  -webkit-font-smoothing:  antialiased;\n  -moz-osx-font-smoothing: grayscale;\n  transition:\n    background-color var(--transition-theme),\n    color            var(--transition-theme);\n}\n\nimg, svg { display: block; max-width: 100%; }\n\na {\n  color: var(--color-text-dim);\n  text-decoration: none;\n  transition: color var(--transition-fast);\n}\na:hover { color: var(--color-text); }\n\nsection {\n  padding: var(--space-9) 0;\n  border-bottom: 1px dashed var(--color-border);\n  transition: border-color var(--transition-base);\n}\nsection:last-child { border-bottom: none; }\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  10  TYPOGRAPHY ELEMENTS\n *\n *  Heading font:  DM Mono\n *  ─── H1–H3  weight 300, letter-spacing as defined per level\n *  ─── H4–H6  weight 400, letter-spacing as defined per level\n *  ─── H6     also text-transform: uppercase\n *\n *  Body:   Inter 400  16px  line-height 1.75\n *  Mono:   DM Mono 400  12px  tracking 0.05em\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n/* ── Heading base ─────────────────────────────────────────────────────────── */\nh1, h2, h3, h4, h5, h6,\n.h1, .h2, .h3, .h4, .h5, .h6 {\n  font-family: var(--font-display);\n  line-height:  var(--leading-heading);\n  color:        var(--color-text);\n  transition:   color var(--transition-theme);\n}\n\n/* H1–H3: Light (300) — tight to negative tracking */\nh1, .h1 {\n  font-size:      var(--text-h1);          /* 42px */\n  font-weight:    var(--weight-light);     /* 300  */\n  letter-spacing: var(--tracking-h1);      /* -.042em */\n}\n\nh2, .h2 {\n  font-size:      var(--text-h2);          /* 36px */\n  font-weight:    var(--weight-light);     /* 300  */\n  letter-spacing: var(--tracking-h2);      /* -.021em */\n}\n\nh3, .h3 {\n  font-size:      var(--text-h3);          /* 24px */\n  font-weight:    var(--weight-light);     /* 300  */\n  letter-spacing: var(--tracking-h3);      /* -.014em */\n}\n\n/* H4–H6: Regular (400) — neutral to open tracking */\nh4, .h4 {\n  font-size:      var(--text-h4);          /* 21px */\n  font-weight:    var(--weight-regular);   /* 400  */\n  letter-spacing: var(--tracking-h4);      /* -.007em */\n}\n\nh5, .h5 {\n  font-size:      var(--text-h5);          /* 18px */\n  font-weight:    var(--weight-regular);   /* 400  */\n  letter-spacing: var(--tracking-h5);      /* +.007em */\n}\n\nh6, .h6 {\n  font-size:      var(--text-h6);          /* 16px */\n  font-weight:    var(--weight-regular);   /* 400  */\n  letter-spacing: var(--tracking-h6);      /* +.042em */\n  text-transform: uppercase;\n}\n\n/* ── Body text ────────────────────────────────────────────────────────────── */\np, .body {\n  font-family: var(--font-body);\n  font-size:   var(--text-base);\n  font-weight: var(--weight-regular);\n  line-height: var(--leading-body);\n  color:       var(--color-text-muted);\n  transition:  color var(--transition-theme);\n}\n\n.body-sm {\n  font-size:   var(--text-sm);\n  line-height: var(--leading-tight);\n}\n\n/* ── Monospace / technical ───────────────────────────────────────────────── */\ncode,\n.mono {\n  font-family:    var(--font-mono);\n  font-size:      var(--text-xs);          /* 12px */\n  font-weight:    var(--weight-regular);\n  letter-spacing: var(--tracking-mono);    /* .05em */\n  color:          var(--color-text-dim);\n  transition:     color var(--transition-theme);\n}\n\n/* ── Section tag / eyebrow (DM Mono, 10px, allcaps, wide tracking) ────────── */\n.section-tag {\n  display:        block;\n  font-family:    var(--font-mono);\n  font-size:      var(--text-label);       /* 10px */\n  font-weight:    var(--weight-regular);\n  letter-spacing: var(--tracking-wide);    /* .20em */\n  text-transform: uppercase;\n  color:          var(--color-text-dim);\n  margin-bottom:  10px;\n  transition:     color var(--transition-theme);\n}\n\n/* ── Chapter number ─────────────────────────────────────────────────────── */\n.ch-num {\n  font-family:    var(--font-mono);\n  font-size:      var(--text-xs);          /* 12px */\n  color:          var(--color-text-dim);\n  letter-spacing: var(--tracking-wide);\n  flex-shrink:    0;\n  padding-top:    4px;\n  transition:     color var(--transition-theme);\n}\n\n.chapter {\n  display:       flex;\n  align-items:   baseline;\n  gap:           20px;\n  margin-bottom: 16px;\n}\n\n/* ── Rule / separator ───────────────────────────────────────────────────── */\n.rule {\n  width:      40px;\n  height:     1px;\n  background: var(--color-border);\n  margin:     24px 0;\n  border:     none;\n  transition: background var(--transition-theme);\n}\n.rule-wide { width: 80px; }\n\nhr {\n  border:     none;\n  height:     1px;\n  background: var(--color-border);\n  margin:     var(--space-5) 0;\n  transition: background var(--transition-theme);\n}\n\n/* ── Type specimens (brand manual only) ─────────────────────────────────── */\n.type-s {\n  background:    var(--color-surface);\n  border:        1px solid var(--color-border);\n  border-radius: var(--radius-md);\n  padding:       24px 28px;\n  margin-bottom: 2px;\n  transition:\n    background var(--transition-theme),\n    border-color var(--transition-theme);\n}\n.type-s .meta {\n  font-family:    var(--font-mono);\n  font-size:      var(--text-label);\n  color:          var(--color-text-dim);\n  margin-bottom:  12px;\n  display:        block;\n  letter-spacing: var(--tracking-mono);\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  11  SURFACES\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n.card {\n  background:    var(--color-surface);\n  border:        1px solid var(--color-border);\n  border-radius: var(--radius-md);   /* 10px */\n  padding:       var(--space-6);     /* 32px */\n  transition:\n    background   var(--transition-theme),\n    border-color var(--transition-theme);\n}\n\n.card-sm {\n  background:    var(--color-surface);\n  border:        1px solid var(--color-border);\n  border-radius: var(--radius-md);\n  padding:       var(--space-4);     /* 16px */\n  transition:\n    background   var(--transition-theme),\n    border-color var(--transition-theme);\n}\n\n.card-inset {\n  background:    var(--color-bg);\n  border:        1px solid var(--color-border-sub);\n  border-radius: var(--radius-md);\n  padding:       var(--space-5);\n}\n\n.demo-box {\n  background:    var(--color-surface);\n  border:        1px solid var(--color-border);\n  border-radius: var(--radius-md);\n  padding:       36px;\n  transition:\n    background   var(--transition-theme),\n    border-color var(--transition-theme);\n}\n\n/* Fixed dark/light panel pair (always locked regardless of theme toggle) */\n.panel-dark  {\n  background:    #111213;\n  border:        1px solid #3C3C3C;\n  border-radius: var(--radius-md);\n}\n.panel-light {\n  background:    #F2F2EF;\n  border:        1px solid #D0D0CC;\n  border-radius: var(--radius-md);\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  12  NAVIGATION\n * ═══════════════════════════════════════════════════════════════════════════ */\n\nnav {\n  position:          sticky;\n  top:               0;\n  z-index:           100;\n  background:        var(--color-nav-bg);\n  backdrop-filter:   blur(16px);\n  -webkit-backdrop-filter: blur(16px);\n  border-bottom:     1px solid var(--color-nav-border);\n  display:           flex;\n  align-items:       stretch;\n  transition:\n    background   var(--transition-theme),\n    border-color var(--transition-theme);\n}\n\n.nav-links {\n  display:      flex;\n  list-style:   none;\n  overflow-x:   auto;\n  flex:         1;\n  min-width:    0;\n  scrollbar-width: none;\n}\n.nav-links::-webkit-scrollbar { display: none; }\n\n.nav-links li a,\n.nav-link {\n  display:        block;\n  padding:        16px;\n  font-family:    var(--font-mono);\n  font-size:      var(--text-label);    /* 10px */\n  letter-spacing: var(--tracking-label);/* .15em */\n  text-transform: uppercase;\n  color:          var(--color-text-dim);\n  text-decoration: none;\n  white-space:    nowrap;\n  transition:     color var(--transition-fast);\n}\n.nav-links li a:hover,\n.nav-link:hover { color: var(--color-text); }\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  13  BUTTONS — BASE\n *\n *  Normal: h 42px · r 10px · Inter Medium 16px\n *  Small:  h 36px · r  5px · Inter Medium 12px\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n.btn,\n.btn-sm {\n  display:         inline-flex;\n  align-items:     center;\n  justify-content: center;\n  gap:             var(--space-2);\n  cursor:          pointer;\n  border:          none;\n  white-space:     nowrap;\n  font-family:     var(--font-body);\n  font-weight:     var(--weight-medium);  /* Inter 500 */\n  text-decoration: none;\n  transition:\n    opacity       var(--transition-fast),\n    transform     var(--transition-fast),\n    background    var(--transition-base),\n    color         var(--transition-base),\n    border-color  var(--transition-base),\n    box-shadow    var(--transition-base);\n}\n\n/* Normal */\n.btn {\n  height:        42px;\n  padding:       0 24px;\n  border-radius: var(--radius-md);   /* 10px */\n  font-size:     var(--text-base);   /* 16px */\n}\n\n/* Small */\n.btn-sm {\n  height:        36px;\n  padding:       0 16px;\n  border-radius: var(--radius-sm);   /* 5px  */\n  font-size:     12px;\n}\n\n.btn:hover,\n.btn-sm:hover  { opacity: .88; }\n\n.btn:active,\n.btn-sm:active { transform: scale(.98); }\n\n.btn:disabled,\n.btn-sm:disabled {\n  opacity:        .30;\n  cursor:         not-allowed;\n  pointer-events: none;\n}\n\n/* Icons inside buttons */\n.btn    svg, .btn    ion-icon { font-size: 18px; flex-shrink: 0; }\n.btn-sm svg, .btn-sm ion-icon { font-size: 14px; flex-shrink: 0; }\n\n/* Ghost variants */\n.btn-ghost    { background: transparent; border: 1px solid var(--color-ghost-border); color: var(--color-ghost-text); }\n.btn-ghost-sm { background: transparent; border: 1px solid var(--color-ghost-border); color: var(--color-ghost-text); }\n.btn-ghost:hover,\n.btn-ghost-sm:hover { opacity: .88; }\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  14  BUTTONS — SHOCKNET (parent / monochrome)\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n.btn-sn,\n.btn-sn-sm  { background: var(--color-sn); color: var(--color-sn-on); }\n.btn-sn:hover,\n.btn-sn-sm:hover { box-shadow: var(--glow-sn); }\n\n.btn-sn-out {\n  background:   transparent;\n  border:       1px solid var(--color-sn);\n  color:        var(--color-sn);\n}\n.btn-sn-out:hover { box-shadow: var(--glow-sn); }\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  15  PILLS\n *\n *  Font: DM Mono 10px, tracking .10em–.15em, ALLCAPS\n *  All product-colored pill variants live in each product's CSS file.\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n.pill {\n  display:        inline-flex;\n  align-items:    center;\n  gap:            5px;\n  padding:        3px 10px;\n  border-radius:  var(--radius-full);\n  font-family:    var(--font-mono);\n  font-size:      var(--text-label);    /* 10px */\n  font-weight:    var(--weight-regular);\n  letter-spacing: 0.10em;\n  text-transform: uppercase;\n  line-height:    1;\n  transition:\n    background   var(--transition-base),\n    border-color var(--transition-base),\n    color        var(--transition-base);\n}\n\n/* Neutral pills */\n.pill-aa  {\n  background: var(--color-bg);\n  border:     1px solid var(--color-border);\n  color:      var(--color-text);\n}\n.pill-def {\n  background: var(--color-surface);\n  border:     1px solid var(--color-border);\n  color:      var(--color-text-muted);\n}\n\n/* Dot indicator modifier */\n.pill--dot::before {\n  content:       '';\n  display:       inline-block;\n  width:         5px;\n  height:        5px;\n  border-radius: 50%;\n  background:    currentColor;\n  flex-shrink:   0;\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  16  DOWNLOAD ROW\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n.dl-row {\n  display:       flex;\n  gap:           10px;\n  flex-wrap:     wrap;\n  padding:       24px 0 0;\n  border-top:    1px solid color-mix(in srgb, var(--color-border) 60%, transparent);\n  margin-top:    24px;\n  transition:    border-color var(--transition-theme);\n}\n\n.dl-label {\n  width:          100%;\n  font-family:    var(--font-mono);\n  font-size:      var(--text-label);\n  letter-spacing: var(--tracking-mono);\n  text-transform: uppercase;\n  margin-bottom:  var(--space-2);\n  color:          var(--color-text-dim);\n  transition:     color var(--transition-theme);\n}\n\n.dl-btn {\n  display:        inline-flex;\n  align-items:    center;\n  gap:            8px;\n  height:         38px;\n  padding:        0 18px;\n  border-radius:  8px;\n  font-family:    var(--font-mono);\n  font-size:      var(--text-label);\n  letter-spacing: 0.10em;\n  text-transform: uppercase;\n  cursor:         pointer;\n  border:         1px solid var(--color-border);\n  background:     var(--color-surface);\n  color:          var(--color-text-muted);\n  text-decoration: none;\n  transition:\n    background   var(--transition-fast),\n    color        var(--transition-fast),\n    border-color var(--transition-fast);\n}\n.dl-btn:hover {\n  border-color: var(--color-text-muted);\n  color:        var(--color-text);\n}\n.dl-btn .dl-dot {\n  width:         8px;\n  height:        8px;\n  border-radius: 50%;\n  flex-shrink:   0;\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  17  SPEC ROW  (definition / key–value pair)\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n.spec-row {\n  display:         flex;\n  justify-content: space-between;\n  align-items:     center;\n  padding:         10px 0;\n  border-bottom:   1px solid color-mix(in srgb, var(--color-border) 50%, transparent);\n  transition:      border-color var(--transition-theme);\n}\n.spec-row:last-child { border-bottom: none; }\n\n.spec-k {\n  font-family:    var(--font-mono);\n  font-size:      var(--text-label);\n  color:          var(--color-text-dim);\n  text-transform: uppercase;\n  letter-spacing: 0.10em;\n}\n.spec-v {\n  font-family: var(--font-mono);\n  font-size:   var(--text-label);\n  color:       var(--color-text);\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  18  CALLOUT\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n.callout {\n  display:       flex;\n  align-items:   flex-start;\n  gap:           14px;\n  padding:       18px 22px;\n  border-radius: var(--radius-md);\n  margin-bottom: 28px;\n  background:    color-mix(in srgb, var(--color-text) 5%,  transparent);\n  border:        1px solid color-mix(in srgb, var(--color-text) 15%, transparent);\n  transition:\n    background   var(--transition-theme),\n    border-color var(--transition-theme);\n}\n\n.callout-icon {\n  font-size:   17px;\n  flex-shrink: 0;\n  margin-top:  2px;\n}\n\n.callout h6 { margin-bottom: 4px; font-size: 12px; letter-spacing: .04em; color: var(--color-text); }\n.callout p  { font-size: 13px; color: var(--color-text-muted); }\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  19  LOGO BOX & MARK PAIR\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n.logo-box {\n  background:      var(--color-surface);\n  border:          1px solid var(--color-border);\n  border-radius:   var(--radius-md);\n  padding:         48px 40px;\n  display:         flex;\n  align-items:     center;\n  justify-content: center;\n  min-height:      140px;\n  transition:\n    background   var(--transition-theme),\n    border-color var(--transition-theme);\n}\n\n.mark-pair {\n  display:               grid;\n  grid-template-columns: 1fr 1fr;\n  gap:                   10px;\n}\n\n/* Fixed-background cells — always locked, independent of theme toggle */\n.mark-dark {\n  background:    #111213;\n  border:        1px solid #3C3C3C;\n  border-radius: var(--radius-md);\n  padding:       20px;\n  display:       flex;\n  justify-content: center;\n  align-items:   center;\n  min-height:    110px;\n}\n.mark-light {\n  background:    #F2F2EF;\n  border:        1px solid #D0D0CC;\n  border-radius: var(--radius-md);\n  padding:       20px;\n  display:       flex;\n  justify-content: center;\n  align-items:   center;\n  min-height:    110px;\n}\n\n.product-bar {\n  width:         4px;\n  height:        44px;\n  border-radius: 2px;\n  flex-shrink:   0;\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  20  COLOR SWATCHES\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n/* Single swatch */\n.sw-grid {\n  display:               grid;\n  grid-template-columns: repeat(auto-fill, minmax(148px, 1fr));\n  gap:                   12px;\n}\n.swatch {\n  border-radius: 8px;\n  overflow:      hidden;\n  border:        1px solid var(--color-border);\n  transition:    border-color var(--transition-theme);\n}\n.swatch-c { height: 68px; }\n.swatch-i {\n  background: var(--color-surface);\n  padding:    10px 12px;\n  transition: background var(--transition-theme);\n}\n.swatch-i .nm { font-size: 11px; font-weight: 500; color: var(--color-text); margin-bottom: 2px; }\n.swatch-i .hx { font-family: var(--font-mono); font-size: 10px; color: var(--color-text-muted); }\n\n/* Split swatch (dark left / light right — always fixed) */\n.cmp-grid {\n  display:               grid;\n  grid-template-columns: repeat(auto-fill, minmax(222px, 1fr));\n  gap:                   12px;\n}\n.cmp-swatch {\n  border-radius: var(--radius-md);\n  overflow:      hidden;\n  border:        1px solid var(--color-border);\n  transition:    border-color var(--transition-theme);\n}\n.cmp-halves { display: flex; }\n.cmp-h { height: 56px; flex: 1; }\n.cmp-body {\n  background: var(--color-surface);\n  padding:    12px 14px;\n  transition: background var(--transition-theme);\n}\n.cmp-body .cn { font-size: 11px; font-weight: 500; color: var(--color-text); margin-bottom: 7px; }\n.cmp-row  { display: flex; align-items: center; gap: 7px; margin-bottom: 4px; }\n.cmp-dot  { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }\n.cmp-val  { font-family: var(--font-mono); font-size: 10px; color: var(--color-text-muted); flex: 1; }\n.cmp-tag  { font-family: var(--font-mono); font-size: 8px; text-transform: uppercase; letter-spacing: .08em; padding: 1px 5px; border-radius: 3px; }\n.tag-d    { background: rgba(255,255,255,.08); color: #888; }\n.tag-l    { background: rgba(0,0,0,.07);       color: #888; }\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  21  CHAPTER / SECTION LABELS\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n.icon-section-header {\n  display:       flex;\n  align-items:   center;\n  gap:           10px;\n  margin-bottom: 20px;\n}\n.icon-section-header__bar {\n  width:         3px;\n  height:        20px;\n  border-radius: 2px;\n  flex-shrink:   0;\n}\n.icon-section-header__label {\n  font-family:    var(--font-mono);\n  font-size:      var(--text-label);\n  letter-spacing: var(--tracking-label);\n  text-transform: uppercase;\n  color:          var(--color-text-dim);\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  22  ICON CELLS & ICON GRID\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n.icon-grid {\n  display:               grid;\n  grid-template-columns: repeat(auto-fill, minmax(72px, 1fr));\n  gap:                   4px;\n}\n\n.icon-cell {\n  display:         flex;\n  flex-direction:  column;\n  align-items:     center;\n  gap:             8px;\n  padding:         14px 6px;\n  border-radius:   8px;\n  transition:      background var(--transition-fast);\n  cursor:          default;\n}\n.icon-cell:hover { background: color-mix(in srgb, var(--color-text) 6%, transparent); }\n\n.icon-cell__glyph { font-size: 22px; }\n.icon-cell__label {\n  font-family:    var(--font-mono);\n  font-size:      8px;\n  color:          var(--color-text-dim);\n  text-align:     center;\n  letter-spacing: .04em;\n  line-height:    1.3;\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  23  APP ICONS & FAVICONS\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n.app-icon {\n  width:           72px;\n  height:          72px;\n  border-radius:   18px;   /* iOS/Android 22% corner radius */\n  display:         flex;\n  align-items:     center;\n  justify-content: center;\n  flex-shrink:     0;\n}\n\n.icon-label {\n  font-family:    var(--font-mono);\n  font-size:      9px;\n  color:          var(--color-text-dim);\n  text-transform: uppercase;\n  letter-spacing: .08em;\n  text-align:     center;\n}\n\n/* Favicon cells */\n.fav-cell { display: flex; flex-direction: column; align-items: center; gap: 8px; }\n.fav-box {\n  background:    #111213;\n  border-radius: 4px;\n  display:       flex;\n  align-items:   center;\n  justify-content: center;\n  border:        1px solid #3C3C3C;\n}\n.fav-light-box {\n  background:    #F2F2EF;\n  border-radius: 4px;\n  display:       flex;\n  align-items:   center;\n  justify-content: center;\n  border:        1px solid #D0D0CC;\n}\n.favicon-row { display: flex; align-items: flex-end; gap: 20px; margin-bottom: 40px; }\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  24  FORM ELEMENTS\n * ═══════════════════════════════════════════════════════════════════════════ */\n\ninput[type=\"text\"],\ninput[type=\"email\"],\ninput[type=\"password\"],\ninput[type=\"number\"],\ninput[type=\"search\"],\ninput[type=\"url\"],\ntextarea,\nselect {\n  display:        block;\n  width:          100%;\n  height:         42px;\n  padding:        0 16px;\n  background:     var(--color-surface);\n  border:         1px solid var(--color-border);\n  border-radius:  var(--radius-md);\n  font-family:    var(--font-body);\n  font-size:      var(--text-base);\n  font-weight:    var(--weight-regular);\n  color:          var(--color-text);\n  outline:        none;\n  -webkit-appearance: none;\n  appearance:     none;\n  transition:\n    background   var(--transition-theme),\n    border-color var(--transition-fast),\n    box-shadow   var(--transition-fast),\n    color        var(--transition-theme);\n}\ntextarea {\n  height:      auto;\n  min-height:  100px;\n  padding:     12px 16px;\n  resize:      vertical;\n  line-height: var(--leading-body);\n}\ninput::placeholder,\ntextarea::placeholder { color: var(--color-text-dim); }\n\n/* Generic focus — product files override this with their accent */\ninput:focus,\ntextarea:focus,\nselect:focus {\n  border-color: var(--color-text-muted);\n  box-shadow:   0 0 0 2px color-mix(in srgb, var(--color-text-muted) 18%, transparent);\n}\n\n.form-label {\n  display:        block;\n  font-family:    var(--font-mono);\n  font-size:      var(--text-label);\n  letter-spacing: 0.12em;\n  text-transform: uppercase;\n  color:          var(--color-text-dim);\n  margin-bottom:  var(--space-2);\n}\n.form-group  { margin-bottom: var(--space-5); }\n.form-hint   { font-family: var(--font-mono); font-size: 11px; color: var(--color-text-dim); margin-top: var(--space-1); }\n.form-error  { font-family: var(--font-mono); font-size: 11px; color: var(--color-danger); margin-top: var(--space-1); }\n.form-group--error input,\n.form-group--error textarea { border-color: var(--color-danger); }\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  25  THEME TOGGLE\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n.toggle-wrap {\n  display:     flex;\n  align-items: center;\n  gap:         10px;\n  padding:     0 18px;\n  border-left: 1px solid var(--color-border);\n  flex-shrink: 0;\n  transition:  border-color var(--transition-theme);\n}\n\n.toggle-name {\n  font-family:    var(--font-mono);\n  font-size:      var(--text-label);\n  letter-spacing: 0.12em;\n  text-transform: uppercase;\n  color:          var(--color-text-dim);\n  min-width:      38px;\n  transition:     color var(--transition-theme);\n}\n\n.toggle-btn {\n  position:      relative;\n  width:         52px;\n  height:        28px;\n  border-radius: var(--radius-full);\n  background:    var(--color-surface);\n  border:        1px solid var(--color-border);\n  cursor:        pointer;\n  flex-shrink:   0;\n  outline:       none;\n  transition:\n    background   var(--transition-base),\n    border-color var(--transition-theme);\n}\n.toggle-btn::after {\n  content:       '';\n  position:      absolute;\n  top:           4px;\n  left:          4px;\n  width:         18px;\n  height:        18px;\n  border-radius: 50%;\n  background:    var(--color-text);\n  transition:\n    transform  var(--transition-spring),\n    background var(--transition-theme);\n}\n[data-theme=\"light\"] .toggle-btn::after { transform: translateX(24px); }\n\n/* Inline icon labels inside toggle */\n.ti          { position: absolute; font-size: 11px; top: 50%; transform: translateY(-50%); pointer-events: none; }\n.ti-m        { left: 7px; }\n.ti-s        { right: 6px; }\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  26  DO / DON'T GRID  (brand manual logo rules section)\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n.do-dont-grid {\n  display:               grid;\n  grid-template-columns: repeat(3, 1fr);\n  gap:                   12px;\n}\n\n.dont-cell {\n  border-radius: var(--radius-md);\n  overflow:      hidden;\n  border:        1px solid var(--color-border);\n  background:    var(--color-surface);\n}\n.dont-preview {\n  height:          110px;\n  background:      #111213;\n  display:         flex;\n  align-items:     center;\n  justify-content: center;\n  position:        relative;\n}\n.dont-preview.light-bg { background: #F2F2EF; }\n\n.dont-badge {\n  position:        absolute;\n  top:             10px;\n  right:           10px;\n  width:           22px;\n  height:          22px;\n  border-radius:   50%;\n  background:      #FF0050;\n  display:         flex;\n  align-items:     center;\n  justify-content: center;\n  font-size:       11px;\n  font-weight:     700;\n  color:           #fff;\n  flex-shrink:     0;\n}\n.do-badge { background: #25E673; color: #111213; }\n\n.dont-label         { padding: 12px 14px; }\n.dont-label p       { font-size: 12px; color: var(--color-text-muted); }\n.dont-label .dl-head {\n  font-family:    var(--font-mono);\n  font-size:      var(--text-label);\n  text-transform: uppercase;\n  letter-spacing: 0.10em;\n  color:          var(--color-text);\n  margin-bottom:  3px;\n}\n\n/* Clear space demo */\n.clearspace-demo { position: relative; display: inline-flex; align-items: center; justify-content: center; }\n.cs-box { position: absolute; inset: 0; border: 1.5px dashed rgba(27,207,224,.50); border-radius: 2px; pointer-events: none; }\n.cs-tick { position: absolute; font-family: var(--font-mono); font-size: 9px; color: #1BCFE0; white-space: nowrap; }\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  27  VOICE & TONE  (brand manual section 12)\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n.voice-pillars {\n  display:               grid;\n  grid-template-columns: repeat(4, 1fr);\n  gap:                   12px;\n  margin-bottom:         40px;\n}\n\n.voice-pill-card {\n  border-radius: var(--radius-md);\n  border:        1px solid var(--color-border);\n  background:    var(--color-surface);\n  padding:       24px 20px;\n  transition:\n    background   var(--transition-theme),\n    border-color var(--transition-theme);\n}\n.vpc-num {\n  font-family:    var(--font-mono);\n  font-size:      9px;\n  color:          var(--color-text-dim);\n  letter-spacing: var(--tracking-wide);\n  margin-bottom:  12px;\n  display:        block;\n}\n.vpc-h {\n  font-size:   var(--text-h5);\n  font-family: var(--font-display);\n  font-weight: var(--weight-regular);\n  color:       var(--color-text);\n  margin-bottom: 8px;\n}\n.vpc-p { font-size: 12px; color: var(--color-text-muted); line-height: 1.6; }\n\n.tone-matrix { width: 100%; border-collapse: collapse; margin-bottom: 40px; }\n.tone-matrix th {\n  font-family:    var(--font-mono);\n  font-size:      9px;\n  text-transform: uppercase;\n  letter-spacing: 0.12em;\n  color:          var(--color-text-dim);\n  padding:        10px 14px;\n  text-align:     left;\n  border-bottom:  1px solid var(--color-border);\n}\n.tone-matrix td {\n  padding:        14px 14px;\n  border-bottom:  1px solid color-mix(in srgb, var(--color-border) 50%, transparent);\n  font-size:      13px;\n  color:          var(--color-text-muted);\n  vertical-align: top;\n}\n.tone-matrix td:first-child { font-family: var(--font-mono); font-size: 11px; color: var(--color-text); font-weight: 500; white-space: nowrap; }\n.tone-matrix tr:last-child td { border-bottom: none; }\n\n.copy-example {\n  background:    var(--color-surface);\n  border:        1px solid var(--color-border);\n  border-radius: var(--radius-md);\n  padding:       24px 28px;\n  margin-bottom: 2px;\n  transition:\n    background   var(--transition-theme),\n    border-color var(--transition-theme);\n}\n.ce-label { font-family: var(--font-mono); font-size: 9px; text-transform: uppercase; letter-spacing: 0.12em; color: var(--color-text-dim); display: block; margin-bottom: 10px; }\n.ce-bad   { font-size: 14px; color: #FF0050; text-decoration: line-through; margin-bottom: 8px; }\n.ce-good  { font-size: 14px; color: var(--color-text); font-weight: 500; }\n.ce-note  { font-size: 11px; color: var(--color-text-dim); margin-top: 6px; }\n\n.word-cols     { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-bottom: 40px; }\n.word-list-box { border-radius: var(--radius-md); border: 1px solid var(--color-border); background: var(--color-surface); padding: 24px; }\n.wl-head       { display: flex; align-items: center; gap: 8px; margin-bottom: 16px; }\n.wl-dot        { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }\n.wl-dot-green  { background: #25E673; }\n.wl-dot-red    { background: #FF0050; }\n.wl-h          { font-family: var(--font-mono); font-size: var(--text-label); text-transform: uppercase; letter-spacing: 0.12em; color: var(--color-text); }\n.word-chip     { display: inline-flex; align-items: center; padding: 4px 10px; border-radius: 5px; font-size: 12px; margin: 3px; font-family: var(--font-mono); letter-spacing: .04em; }\n.chip-use      { background: color-mix(in srgb, #25E673 12%, transparent); border: 1px solid color-mix(in srgb, #25E673 30%, transparent); color: #25E673; }\n.chip-avoid    { background: color-mix(in srgb, #FF0050 12%, transparent); border: 1px solid color-mix(in srgb, #FF0050 30%, transparent); color: #FF0050; }\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  28  CO-BRANDING\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n.cobranding-rule {\n  display:       flex;\n  align-items:   flex-start;\n  gap:           14px;\n  padding:       16px 20px;\n  border-radius: 8px;\n  border:        1px solid var(--color-border);\n  margin-bottom: 8px;\n  background:    var(--color-surface);\n  transition:\n    background   var(--transition-theme),\n    border-color var(--transition-theme);\n}\n.cbr-num  { font-family: var(--font-mono); font-size: var(--text-label); color: var(--color-text-dim); letter-spacing: 0.10em; width: 24px; flex-shrink: 0; padding-top: 2px; }\n.cbr-body h6 { font-size: 13px; letter-spacing: .04em; color: var(--color-text); margin-bottom: 3px; }\n.cbr-body p  { font-size: 13px; color: var(--color-text-muted); }\n\n.excl-zone   { position: relative; display: inline-flex; align-items: center; justify-content: center; }\n.excl-border { position: absolute; inset: -20px; border: 1.5px dashed rgba(255,0,80,.45); border-radius: 4px; pointer-events: none; }\n.excl-tick   { position: absolute; font-family: var(--font-mono); font-size: 8px; color: #FF0050; }\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  29  SVG HELPERS\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n/* Wordmark fill — adapts to active theme */\n.wm { fill: var(--color-text); transition: fill var(--transition-theme); }\n\n/* Decorative background grid (cover section) */\n.cover-grid {\n  position: absolute;\n  inset:    0;\n  pointer-events: none;\n  background-image:\n    linear-gradient(var(--color-grid-line) 1px, transparent 1px),\n    linear-gradient(90deg, var(--color-grid-line) 1px, transparent 1px);\n  background-size: 60px 60px;\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  30  FOOTER\n * ═══════════════════════════════════════════════════════════════════════════ */\n\nfooter {\n  padding:    var(--space-7) var(--space-7);\n  background: var(--color-bg);\n  border-top: 1px solid var(--color-border);\n  transition:\n    background   var(--transition-theme),\n    border-color var(--transition-theme);\n}\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  31  UTILITIES\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n/* Display */\n.flex          { display: flex; }\n.flex-col      { display: flex; flex-direction: column; }\n.flex-center   { display: flex; align-items: center; justify-content: center; }\n.flex-between  { display: flex; align-items: center; justify-content: space-between; }\n.flex-wrap     { flex-wrap: wrap; }\n.inline-flex   { display: inline-flex; }\n.hidden        { display: none; }\n\n/* Gap */\n.gap-1 { gap: var(--space-1); } .gap-2 { gap: var(--space-2); }\n.gap-3 { gap: var(--space-3); } .gap-4 { gap: var(--space-4); }\n.gap-5 { gap: var(--space-5); } .gap-6 { gap: var(--space-6); }\n\n/* Alignment */\n.items-center    { align-items: center; }\n.items-start     { align-items: flex-start; }\n.items-end       { align-items: flex-end; }\n.justify-center  { justify-content: center; }\n.justify-end     { justify-content: flex-end; }\n.justify-between { justify-content: space-between; }\n\n/* Text */\n.text-center  { text-align: center; }\n.text-right   { text-align: right; }\n.text-upper   { text-transform: uppercase; }\n.text-color   { color: var(--color-text); }\n.text-muted   { color: var(--color-text-muted); }\n.text-dim     { color: var(--color-text-dim); }\n\n/* Spacing */\n.mt-1{margin-top:var(--space-1)}.mt-2{margin-top:var(--space-2)}.mt-3{margin-top:var(--space-3)}\n.mt-4{margin-top:var(--space-4)}.mt-5{margin-top:var(--space-5)}.mt-6{margin-top:var(--space-6)}\n.mt-7{margin-top:var(--space-7)}.mt-8{margin-top:var(--space-8)}\n.mb-1{margin-bottom:var(--space-1)}.mb-2{margin-bottom:var(--space-2)}.mb-3{margin-bottom:var(--space-3)}\n.mb-4{margin-bottom:var(--space-4)}.mb-5{margin-bottom:var(--space-5)}.mb-6{margin-bottom:var(--space-6)}\n.mb-7{margin-bottom:var(--space-7)}.mb-8{margin-bottom:var(--space-8)}\n.p-4{padding:var(--space-4)}.p-5{padding:var(--space-5)}.p-6{padding:var(--space-6)}\n.px-4{padding-inline:var(--space-4)}.py-4{padding-block:var(--space-4)}\n.px-5{padding-inline:var(--space-5)}.py-5{padding-block:var(--space-5)}\n.px-6{padding-inline:var(--space-6)}.py-6{padding-block:var(--space-6)}\n\n/* Width */\n.w-full      { width: 100%; }\n.w-auto      { width: auto; }\n.flex-1      { flex: 1; }\n.flex-shrink-0 { flex-shrink: 0; }\n.min-w-0     { min-width: 0; }\n\n/* Border */\n.border   { border: 1px solid var(--color-border); }\n.border-t { border-top: 1px solid var(--color-border); }\n.border-b { border-bottom: 1px solid var(--color-border); }\n\n/* Overflow */\n.overflow-hidden { overflow: hidden; }\n.overflow-x-auto { overflow-x: auto; }\n.truncate        { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }\n\n\n/* ═══════════════════════════════════════════════════════════════════════════\n *  32  RESPONSIVE BREAKPOINTS\n * ═══════════════════════════════════════════════════════════════════════════ */\n\n@media (max-width: 1024px) {\n  :root { --text-h1: 36px; --text-h2: 30px; --text-h3: 22px; }\n  .page { padding-inline: var(--page-pad-md); }\n  .g2 { grid-template-columns: 1fr; }\n  section { padding: 64px 0; }\n  .voice-pillars { grid-template-columns: 1fr 1fr; }\n  .do-dont-grid  { grid-template-columns: 1fr 1fr; }\n}\n\n@media (max-width: 768px) {\n  :root { --text-h1: 28px; --text-h2: 24px; --text-h3: 20px; }\n  .page { padding-inline: var(--page-pad-sm); }\n  .g3  { grid-template-columns: 1fr; }\n  .logo-box { padding: 32px 24px; }\n  .mark-pair { grid-template-columns: 1fr 1fr; }\n  footer { padding: var(--space-6); }\n}\n\n@media (max-width: 480px) {\n  :root { --text-h1: 24px; --text-h2: 20px; --text-h3: 18px; --text-base: 15px; }\n  .g2, .g3, .g4 { grid-template-columns: 1fr; }\n  .mark-pair     { grid-template-columns: 1fr; }\n  .dl-row        { flex-direction: column; }\n  .dl-btn        { width: 100%; justify-content: center; }\n  .voice-pillars { grid-template-columns: 1fr; }\n  .do-dont-grid  { grid-template-columns: 1fr; }\n}\n\n/*\n * ═══════════════════════════════════════════════════════════════════════════\n *  END — shocknet-global.css  ·  v1.2\n * ═══════════════════════════════════════════════════════════════════════════\n */\n";

var shocknetSanctum = "/*\n * ═══════════════════════════════════════════════════════════════════════════\n *  SHOCKNET — SANCTUM  ·  Product Stylesheet\n *  shocknet-sanctum.css  ·  v1.2\n * ═══════════════════════════════════════════════════════════════════════════\n *\n *  LOAD AFTER shocknet-global.css\n *\n *  Colors\n *  ─────────────────────────────────────────────────────────────────────────\n *  Single hue — both mark paths share the same color.\n *  Emerald   dark #25E673   light #4FD27A\n *\n *  Mark orientation: FLIPPED vs all other products\n *  (Path A is the lower arc, Path B is the upper arc in SANCTUM's SVG)\n *\n *  Product: Private encrypted communications on Lightning.\n *\n * ═══════════════════════════════════════════════════════════════════════════\n */\n\n\n/* ── Raw color variables ────────────────────────────────────────────────── */\n\n:root,\n[data-theme=\"dark\"] {\n  --sa-a:    #25E673;   /* Emerald · Vault · Ground */\n  --sa-on:   #111213;\n}\n\n[data-theme=\"light\"] {\n  --sa-a:    #4FD27A;\n  --sa-on:   #111213;\n}\n\n\n/* ── Semantic product tokens ────────────────────────────────────────────── */\n\n[data-product=\"sanctum\"] {\n  --prod-a:        var(--sa-a);\n  --prod-b:        var(--sa-a);\n  --prod-on-a:     var(--sa-on);\n  --prod-on-b:     var(--sa-on);\n  --prod-gradient: var(--sa-a);\n}\n\n\n/* ── Buttons ────────────────────────────────────────────────────────────── */\n\n.btn-sa,\n.btn-sa-sm {\n  background: var(--sa-a);\n  color:      var(--sa-on);\n}\n.btn-sa:hover,\n.btn-sa-sm:hover { box-shadow: 0 0 20px rgba(37,230,115,.20); }\n\n.btn-sa-out {\n  background:    transparent;\n  border:        1px solid var(--sa-a);\n  color:         var(--sa-a);\n}\n.btn-sa-out:hover { box-shadow: 0 0 20px rgba(37,230,115,.20); }\n\n\n/* ── Pills ──────────────────────────────────────────────────────────────── */\n\n.pill-sa {\n  background:  color-mix(in srgb, var(--sa-a) 14%, transparent);\n  border:      1px solid var(--sa-a);\n  color:       var(--sa-a);\n}\n\n[data-theme=\"light\"] .pill-sa { color: #2D9A55; border-color: #4FD27A; background: color-mix(in srgb, #4FD27A 14%, transparent); }\n\n\n/* ── Callout tint ───────────────────────────────────────────────────────── */\n\n.callout--sa {\n  background:   color-mix(in srgb, var(--sa-a) 6%,  transparent);\n  border-color: color-mix(in srgb, var(--sa-a) 18%, transparent);\n}\n\n\n/* ── Icon cell hover ────────────────────────────────────────────────────── */\n\n.icon-cell--sa:hover { background: color-mix(in srgb, var(--sa-a) 10%, transparent); }\n\n\n/* ── Form focus ─────────────────────────────────────────────────────────── */\n\n[data-product=\"sanctum\"] input:focus,\n[data-product=\"sanctum\"] textarea:focus,\n[data-product=\"sanctum\"] select:focus {\n  border-color: var(--sa-a);\n  box-shadow:   0 0 0 2px color-mix(in srgb, var(--sa-a) 20%, transparent);\n}\n\n\n/* ── Product bar accent ─────────────────────────────────────────────────── */\n\n.product-bar--sa { background: var(--sa-a); }\n\n/* Widget inline icons — inherit Sanctum emerald from design tokens */\n[data-product=\"sanctum\"] .sanctum-widget-icon {\n  color: var(--sa-a);\n}\n[data-product=\"sanctum\"] .btn-ghost .sanctum-widget-icon {\n  color: var(--sa-a);\n}\n";

/** Map document-level selectors to the shadow host so Shocknet tokens apply inside closed shadow roots. */
function adaptShocknetCssForShadow(css) {
    return css
        .replace(/(^|\n)\s*:root\s*,/g, '$1:host,')
        .replace(/(^|\n)\s*:root\s*\{/g, '$1:host {')
        .replace(/(^|\n)\s*html\s*\{/g, '$1:host {')
        .replace(/(^|\n)\s*body\s*\{/g, '$1:host {')
        .replace(/:host,\s*\n\s*\[data-theme="dark"\]/g, ':host,\n:host([data-theme="dark"]),\n[data-theme="dark"]');
}
const FONT_IMPORT = `@import url('https://fonts.googleapis.com/css2?family=DM+Mono:wght@300;400;500&family=Inter:wght@400;500;600&display=swap');`;
let cachedAdaptedBundle = null;
function getWidgetStylesheetText() {
    if (!cachedAdaptedBundle) {
        const combined = `${shocknetGlobal}\n${shocknetSanctum}`;
        cachedAdaptedBundle = `${FONT_IMPORT}\n${adaptShocknetCssForShadow(combined)}`;
    }
    return cachedAdaptedBundle;
}

function escapeHtml(s) {
    return s
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;');
}
class WidgetController {
    constructor(deps) {
        this.hostElement = null;
        this.shadowRoot = null;
        this.mountNode = null;
        this.destroyed = false;
        this.mounted = false;
        this.themeMode = 'system';
        this.showLogoutButton = true;
        this.status = 'idle';
        this.requestToken = null;
        this.expiresAt = 0;
        this.timer = null;
        this.lastError = null;
        this.promptConfirmLogout = false;
        this.displayIdentifier = '';
        this.clientKeyDisplay = '';
        this.themePreferenceQuery = typeof window !== 'undefined' && typeof window.matchMedia === 'function'
            ? window.matchMedia('(prefers-color-scheme: dark)')
            : null;
        this.handleMountClick = (e) => {
            const el = e.target?.closest('[data-sa-action]');
            if (!el || !this.mountNode?.contains(el))
                return;
            const action = el.getAttribute('data-sa-action');
            switch (action) {
                case 'connect':
                    this.handleConnectClick();
                    break;
                case 'cancel':
                    this.handleCancel();
                    break;
                case 'logout-prompt':
                    this.promptConfirmLogout = true;
                    this.render();
                    break;
                case 'logout-yes':
                    this.handleLogout();
                    break;
                case 'logout-no':
                    this.promptConfirmLogout = false;
                    this.render();
                    break;
                case 'retry':
                    this.handleConnectClick();
                    break;
            }
        };
        this.handleThemePreferenceChange = () => {
            if (this.themeMode === 'system') {
                this.render();
            }
        };
        this.deps = deps;
        this.socket = new AuthSocketClient({
            url: this.deps.websocketUrl,
            getClientKey: () => this.deps.clientKeyStore.getClientKey(),
            onState: (s) => {
                if (s === 'reconnecting')
                    this.setStatus('reconnecting');
                if (s === 'connecting')
                    this.setStatus('connecting');
            },
            onRequestToken: ({ request_token, expires_at }) => {
                this.requestToken = request_token;
                this.expiresAt = expires_at;
                this.openAuthRequest();
                this.setStatus('awaiting_confirmation');
                this.startTimer();
            },
            onError: (msg) => {
                this.lastError = msg;
            }
        });
    }
    mount(options) {
        if (this.destroyed) {
            throw new SanctumDKError('Client already destroyed', ErrorCode.ALREADY_DESTROYED, false);
        }
        const container = document.getElementById(options.containerId);
        if (!container) {
            throw new SanctumDKError(`Container "${options.containerId}" not found`, ErrorCode.WIDGET_CONTAINER_NOT_FOUND, false);
        }
        if (this.mounted) {
            this.unmount();
        }
        this.hostElement = container;
        this.shadowRoot = container.attachShadow({ mode: 'closed' });
        this.mountNode = document.createElement('div');
        this.mountNode.className = 'sanctum-widget-mount';
        this.shadowRoot.appendChild(this.mountNode);
        this.mountNode.addEventListener('click', this.handleMountClick);
        this.themeMode = options.theme ?? 'system';
        this.openAuthWindow = options.openAuthWindow;
        this.showLogoutButton = options.showLogoutButton !== false;
        this.mounted = true;
        if (this.themePreferenceQuery) {
            if (typeof this.themePreferenceQuery.addEventListener === 'function') {
                this.themePreferenceQuery.addEventListener('change', this.handleThemePreferenceChange);
            }
            else {
                this.themePreferenceQuery.addListener(this.handleThemePreferenceChange);
            }
        }
        this.unsubscribeToken = this.deps.events.on('tokenChange', (t) => {
            if (!this.mounted)
                return;
            if (t) {
                this.displayIdentifier = this.formatIdentifier(t.account_identifier || t.identifier);
                this.status = 'authenticated';
                this.render();
            }
            else {
                this.promptConfirmLogout = false;
                this.displayIdentifier = '';
                if (this.status === 'authenticated') {
                    this.status = 'idle';
                }
                this.render();
            }
        });
        void this.bootstrapFromSession();
    }
    unmount() {
        this.socket.abort();
        if (this.timer !== null) {
            clearInterval(this.timer);
            this.timer = null;
        }
        this.unsubscribeToken?.();
        this.unsubscribeToken = undefined;
        if (this.mountNode) {
            this.mountNode.removeEventListener('click', this.handleMountClick);
        }
        if (this.themePreferenceQuery) {
            if (typeof this.themePreferenceQuery.removeEventListener === 'function') {
                this.themePreferenceQuery.removeEventListener('change', this.handleThemePreferenceChange);
            }
            else {
                this.themePreferenceQuery.removeListener(this.handleThemePreferenceChange);
            }
        }
        if (this.shadowRoot) {
            this.shadowRoot.innerHTML = '';
        }
        this.hostElement = null;
        this.shadowRoot = null;
        this.mountNode = null;
        this.mounted = false;
        this.status = 'idle';
        this.promptConfirmLogout = false;
        this.lastError = null;
    }
    setTheme(theme) {
        this.themeMode = theme;
        this.render();
    }
    destroy() {
        this.unmount();
        this.destroyed = true;
    }
    async bootstrapFromSession() {
        if (!this.mounted)
            return;
        try {
            const [token, clientKey] = await Promise.all([
                this.deps.session.getTokenData(),
                this.deps.clientKeyStore.getClientKey()
            ]);
            this.clientKeyDisplay = clientKey;
            if (token) {
                this.displayIdentifier = this.formatIdentifier(token.account_identifier || token.identifier);
                this.status = 'authenticated';
            }
        }
        catch {
            // keep defaults
        }
        this.render();
    }
    effectiveTheme() {
        if (this.themeMode === 'system') {
            return this.themePreferenceQuery?.matches ? 'dark' : 'light';
        }
        return this.themeMode;
    }
    setStatus(state) {
        this.status = state;
        this.deps.events.emit('authStateChanged', state);
        this.render();
    }
    startAuthFlow() {
        if (!this.mounted)
            return;
        this.lastError = null;
        this.socket.abort();
        void this.socket
            .start()
            .then(async (tokens) => {
            await this.deps.session.setTokenData(tokens);
        })
            .catch((err) => {
            if (err instanceof Error && err.message === AUTH_SOCKET_CANCELLED_MESSAGE)
                return;
            if (err instanceof Error && !this.lastError) {
                this.lastError = err.message;
            }
            this.setStatus('error');
        })
            .finally(() => {
            this.stopTimer();
        });
    }
    openAuthRequest() {
        if (!this.requestToken)
            return;
        const authUrl = new URL(`/auth/requestToken/${this.requestToken}`, this.deps.sanctumUrl).toString();
        if (this.openAuthWindow) {
            this.openAuthWindow(authUrl);
            return;
        }
        window.open(authUrl, '_blank');
    }
    startTimer() {
        this.stopTimer();
        this.timer = window.setInterval(() => this.render(), 1000);
    }
    stopTimer() {
        if (this.timer !== null) {
            clearInterval(this.timer);
            this.timer = null;
        }
    }
    secondsLeft() {
        return Math.max(0, Math.round((this.expiresAt - Date.now()) / 1000));
    }
    formatTime(seconds) {
        const mins = Math.floor(seconds / 60);
        const secs = seconds % 60;
        return `${mins}:${secs.toString().padStart(2, '0')}`;
    }
    formatIdentifier(identifier) {
        if (!identifier)
            return '';
        if (identifier.includes('@')) {
            const [localPart, domain] = identifier.split('@');
            if (localPart.length > 3) {
                return `${localPart.slice(0, 3)}...@${domain}`;
            }
            return identifier;
        }
        if (identifier.startsWith('npub')) {
            return `${identifier.slice(0, 10)}...${identifier.slice(-10)}`;
        }
        return identifier;
    }
    /** Inline SVG with fixed size for template strings (trusted source: icons.ts). */
    sizedSvg(svg) {
        return svg.includes(' width=') ? svg : svg.replace('<svg ', '<svg width="20" height="20" ');
    }
    applySpinnerAnimation(svg) {
        if (!svg || !('animate' in svg))
            return;
        svg.animate([{ transform: 'rotate(0deg)' }, { transform: 'rotate(360deg)' }], { duration: 1100, iterations: Infinity, easing: 'linear' });
    }
    handleCancel() {
        this.socket.abort();
        this.stopTimer();
        this.setStatus('idle');
    }
    handleLogout() {
        void this.deps.session.clear();
        this.promptConfirmLogout = false;
        this.setStatus('idle');
    }
    handleConnectClick() {
        if (this.status === 'connecting' || this.status === 'reconnecting' || this.status === 'awaiting_confirmation') {
            return;
        }
        this.lastError = null;
        this.setStatus('connecting');
        void this.startAuthFlow();
    }
    ensureStyles() {
        if (!this.shadowRoot)
            return;
        const styleId = 'sanctum-sdk-widget-styles';
        const styles = getWidgetStylesheetText();
        if (!WidgetController.sharedStyleText) {
            WidgetController.sharedStyleText = styles;
        }
        const supportsConstructableStylesheets = typeof CSSStyleSheet !== 'undefined' &&
            typeof CSSStyleSheet.prototype.replaceSync === 'function' &&
            'adoptedStyleSheets' in this.shadowRoot;
        if (supportsConstructableStylesheets) {
            if (!WidgetController.sharedStylesheet) {
                const stylesheet = new CSSStyleSheet();
                stylesheet.replaceSync(WidgetController.sharedStyleText);
                WidgetController.sharedStylesheet = stylesheet;
            }
            if (!this.shadowRoot.adoptedStyleSheets.includes(WidgetController.sharedStylesheet)) {
                this.shadowRoot.adoptedStyleSheets = [
                    ...this.shadowRoot.adoptedStyleSheets,
                    WidgetController.sharedStylesheet
                ];
            }
            return;
        }
        if (this.shadowRoot.querySelector(`#${styleId}`)) {
            return;
        }
        const styleElement = document.createElement('style');
        styleElement.id = styleId;
        styleElement.textContent = WidgetController.sharedStyleText;
        this.shadowRoot.insertBefore(styleElement, this.shadowRoot.firstChild);
    }
    buildContentHtml() {
        const s = this.status;
        const spin = this.sizedSvg(settingsIcon);
        const hour = this.sizedSvg(hourglassIcon);
        const check = this.sizedSvg(checkMarkIcon);
        const out = this.sizedSvg(logoutIcon);
        switch (s) {
            case 'connecting':
                return `
          <div class="sanctum-alt-view-container flex-col items-center justify-center gap-3 w-full flex-center gap-2">
            <span class="inline-flex items-center sanctum-widget-icon sanctum-spin-wrap">${spin}</span>
            <span class="body-sm text-muted">Connecting to Sanctum...</span>
          </div>`;
            case 'reconnecting':
                return `
          <div class="sanctum-alt-view-container flex-col items-center justify-center gap-3 w-full flex-center gap-2">
            <span class="inline-flex items-center sanctum-widget-icon sanctum-spin-wrap">${spin}</span>
            <span class="body-sm text-muted">Reconnecting...</span>
            <button type="button" class="btn-sm btn-ghost sanctum-cancel-button" data-sa-action="cancel">Cancel</button>
          </div>`;
            case 'awaiting_confirmation':
                return `
          <div class="sanctum-alt-view-container flex-col items-center justify-center gap-3 w-full">
            <div class="body-sm text-muted text-center">Awaiting confirmation in Sanctum</div>
            <div class="flex-center gap-2">
              <span class="inline-flex items-center sanctum-widget-icon">${hour}</span>
              <span class="timer-num mono text-color">${escapeHtml(this.formatTime(this.secondsLeft()))}</span>
            </div>
            <button type="button" class="btn-sm btn-ghost sanctum-cancel-button" data-sa-action="cancel">Cancel</button>
          </div>`;
            case 'authenticated':
                if (this.showLogoutButton && this.promptConfirmLogout) {
                    return `
            <div class="sanctum-alt-view-container flex-col items-center justify-center gap-3 w-full">
              <span class="h6 text-center">Log out?</span>
              <div class="flex-center gap-2">
                <button type="button" class="btn btn-sa sanctum-login-button" data-sa-action="logout-yes">Yes</button>
                <button type="button" class="btn btn-ghost sanctum-login-button" data-sa-action="logout-no">No</button>
              </div>
            </div>`;
                }
                {
                    const top = this.showLogoutButton
                        ? `<div class="flex justify-end w-full">
                   <button type="button" class="btn-sm btn-ghost sanctum-logout-button inline-flex items-center gap-1" data-sa-action="logout-prompt">
                     <span class="inline-flex items-center sanctum-widget-icon">${out}</span> Log out
                   </button>
                 </div>`
                        : '';
                    const clientLine = this.clientKeyDisplay
                        ? `<span class="mono text-dim">client_id-${escapeHtml(this.clientKeyDisplay)}</span>`
                        : '';
                    return `
            <div class="sanctum-alt-view-container flex-col items-center justify-center gap-3 w-full confirmed gap-2">
              ${top}
              <div class="flex-center gap-2">
                <span class="inline-flex items-center sanctum-widget-icon">${check}</span>
                <span class="pill pill-sa pill--dot">Connected</span>
              </div>
              <span class="mono text-dim">${escapeHtml(this.displayIdentifier)}</span>
              ${clientLine}
            </div>`;
                }
            case 'error':
                return `
          <div class="sanctum-alt-view-container flex-col items-center justify-center gap-3 w-full">
            <span class="h6 text-center">An error occured</span>
            <span class="body-sm text-muted text-center">${escapeHtml(this.lastError ?? 'Unknown error')}</span>
            <div class="flex-center">
              <button type="button" class="btn btn-sa sanctum-login-button" data-sa-action="retry">Try again</button>
            </div>
          </div>`;
            default:
                return `
          <div class="sanctum-alt-view-container flex-col items-center justify-center gap-3 w-full flex-center">
            <button type="button" class="btn btn-sa sanctum-login-button" data-sa-action="connect">Connect with Sanctum</button>
          </div>`;
        }
    }
    buildLogoHtml(theme) {
        const powered = this.status !== 'awaiting_confirmation' && this.status !== 'authenticated'
            ? '<span class="mono text-dim">Powered by</span>'
            : '';
        const logoSrc = theme === 'dark' ? img$1 : img;
        return `
      <div class="sanctum-logo flex-col items-center gap-1 w-full">
        ${powered}
        <div class="flex-center">
          <img class="sanctum-widget-logo" src="${logoSrc}" alt="Sanctum" width="132" height="29" />
        </div>
      </div>`;
    }
    render() {
        if (!this.mountNode || !this.hostElement)
            return;
        this.ensureStyles();
        const theme = this.effectiveTheme();
        this.hostElement.setAttribute('data-product', 'sanctum');
        this.hostElement.setAttribute('data-theme', theme);
        const contentHtml = this.buildContentHtml();
        const logoHtml = this.buildLogoHtml(theme);
        this.mountNode.innerHTML = `
      <div class="sanctum-widget-root card card-sm flex-col items-center justify-center gap-3 w-full" data-product="sanctum" data-theme="${theme}">
        ${contentHtml}
        ${logoHtml}
      </div>`;
        if (this.status === 'connecting' || this.status === 'reconnecting') {
            const wrap = this.mountNode.querySelector('.sanctum-spin-wrap svg');
            this.applySpinnerAnimation(wrap);
        }
    }
}
WidgetController.sharedStylesheet = null;
WidgetController.sharedStyleText = null;

const deriveWebsocketUrl = (url) => {
    return url.replace(/^https:/, 'wss:').replace(/^http:/, 'ws:');
};
const assertUrl = (name, value) => {
    try {
        new URL(value);
    }
    catch {
        throw new Error(`${name} is not a valid URL: ${value}`);
    }
};
const createSanctumDK = (config) => {
    assertUrl('url', config.url);
    const websocketUrl = config.websocketUrl ?? deriveWebsocketUrl(config.url);
    assertUrl('websocketUrl', websocketUrl);
    const storageAdapter = config.storageAdapter ?? getDefaultStorageAdapter();
    const tokenDataAdapter = config.tokenDataAdapter ?? createStorageTokenDataAdapter(storageAdapter);
    const events = new TypedEventBus();
    const session = new SessionManager(tokenDataAdapter, events);
    const clientKeyStore = new ClientKeyStore(storageAdapter, config.getClientKey);
    const apiFacade = new ApiFacade({
        baseUrl: config.url,
        session,
        events,
        clientKeyStore,
    });
    const widgetController = new WidgetController({
        sanctumUrl: config.url,
        websocketUrl,
        session,
        events,
        clientKeyStore
    });
    const eventApi = {
        onTokenChange: (handler) => events.on('tokenChange', handler),
        onTokensUpdated: (handler) => events.on('tokensUpdated', handler),
        onReauthRequired: (handler) => events.on('reauthRequired', handler),
        onAuthStateChanged: (handler) => events.on('authStateChanged', handler)
    };
    const sessionApi = {
        getTokenData: () => session.getTokenData(),
        setTokenData: (tokensData) => session.setTokenData(tokensData),
        clear: () => session.clear()
    };
    return {
        api: apiFacade,
        widget: {
            mount: (options) => widgetController.mount(options),
            unmount: () => widgetController.unmount(),
            setTheme: (theme) => widgetController.setTheme(theme)
        },
        session: sessionApi,
        events: eventApi,
        async destroy() {
            widgetController.destroy();
            events.clear();
        }
    };
};

if (typeof window !== 'undefined') {
    window.SanctumDK = {
        createSanctumDK
    };
}

exports.ErrorCode = ErrorCode;
exports.SanctumDKError = SanctumDKError;
exports.createSanctumDK = createSanctumDK;
//# sourceMappingURL=index.js.map
