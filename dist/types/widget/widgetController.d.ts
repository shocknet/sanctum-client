import type { WidgetMountOptions, WidgetTheme } from '../types';
import type { SessionManager } from '../session/sessionManager';
import type { TypedEventBus } from '../core/events';
import type { ClientKeyStore } from '../storage/clientKeyStore';
type WidgetControllerDeps = {
    sanctumUrl: string;
    websocketUrl: string;
    session: SessionManager;
    events: TypedEventBus;
    clientKeyStore: ClientKeyStore;
};
export declare class WidgetController {
    private static sharedStylesheet;
    private static sharedStyleText;
    private hostElement;
    private shadowRoot;
    private mountNode;
    private socket;
    private deps;
    private destroyed;
    private mounted;
    private themeMode;
    private openAuthWindow?;
    private showLogoutButton;
    private status;
    private requestToken;
    private expiresAt;
    private timer;
    private lastError;
    private promptConfirmLogout;
    private displayIdentifier;
    private clientKeyDisplay;
    private unsubscribeToken?;
    private readonly themePreferenceQuery;
    constructor(deps: WidgetControllerDeps);
    mount(options: WidgetMountOptions): void;
    unmount(): void;
    setTheme(theme: WidgetTheme): void;
    destroy(): void;
    private handleMountClick;
    private handleThemePreferenceChange;
    private bootstrapFromSession;
    private effectiveTheme;
    private setStatus;
    private startAuthFlow;
    private openAuthRequest;
    private startTimer;
    private stopTimer;
    private secondsLeft;
    private formatTime;
    private formatIdentifier;
    /** Inline SVG with fixed size for template strings (trusted source: icons.ts). */
    private sizedSvg;
    private applySpinnerAnimation;
    private handleCancel;
    private handleLogout;
    private handleConnectClick;
    private ensureStyles;
    private buildContentHtml;
    private buildLogoHtml;
    private render;
}
export {};
